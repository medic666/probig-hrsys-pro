# 项目定位
- 本系统是面向中小企业的轻量生产级人事与行政管理系统。
- 后端使用GO, 前端使用Bun+Vite+TypeScript, 项目追求轻量快速、安全稳定。
- 使用Go embed实现单二进制部署，服务启动自动初始化默认配置、默认管理员账号与基础权限体系，零配置即可运行。

# 后端架构
- **事件驱动**：所有业务事实以原始事件形式存储；任何结果数据均可由原始事件全量重建，保证数据可追溯、可复现。
- **多层投影聚合**：采用「事件源→基础投影→业务核算→结果汇总」的四级分层模型，上层投影仅依赖下层数据生成，层层抽象分散计算量且方便逐级核算。投影层数据不允许直接修改。
- **审计全覆盖**：事件源操作全程留痕，审计日志仅追加、不可删除，保证数据合规可追溯。
- **容错性设计**：事件源软删除机制，误删数据可一键恢复，操作风险极低。
- **架构总览**：
| 层级 | 统一命名 | 核心内容 | 触发方式 | 核心特性 |
|------|----------|----------|----------|----------|
| 第0层 | 事件源层 | 职务事件、考勤事件、年假权益事件、工资调整事件 | 用户手动录入/导入、系统周期操作生成 | 原始事实，操作留痕，顺序唯一 |
| 第1层 | 基础投影层 | 职务时段快照、日考勤记工时、年假余额、调休余额 | 事件变动后自动重算 | 粒度最细，实时一致；可全量重建 |
| 第2层 | 业务核算层 | 月度考勤核算| 用户手动触发核算 | 专项业务结果；基于基础投影计算 |
| 第3层 | 结果汇总层 | 月度工资总表 | 用户手动触发核算 | 跨模块聚合最终结果；全链路可追溯，支持多版本留存 |

# 前端架构
- **面向多端**: 响应式布局，有卡片和列表两种主要视图结构。卡片视图方便点击，列表视图提供详细的筛选功能，同时面向多端的使用需求。
- **整体理念**：以"人员 × 时间"为前端展示的逻辑主轴，条理清晰的将信息呈递给用户。
- **卡片视图**：
   - 顶层为人员卡片，作为各模块统一的首页布局，展示公司管理以人为轴的理念。人员卡片可以加上不同的徽章组件，显示不同模块的特有信息。
   - 中层为时段聚合卡片，可按年度/月度聚合信息，按颜色传递信息。方便以时间为线索，引导用户精准获取想要的信息。
   - 底层为该业务的最小粒度的原子卡片，因为不同模块常有不同的层级设计，所以：原子卡片可以是对某日度/月度投影的展示；可以是对某单个事件源的展示；也可以像考勤事件的原子卡片那样，是对单日所有考勤事件的集中展示；甚至对于人员管理模块来说，人员卡片本身就是最小粒度的原子卡片。
   - 点击原子卡片，或者点击原子卡片上的操作按钮，可以进入最终特定的业务逻辑页。
- **列表视图**：可多维度进行筛选/排序，支持导出。

# 项目理念
- **颗粒度统一**：后端考勤事件按小时为精度计算，前端展示按天为单位展示。
- **用户和权限**：权限维度以"授权点类型（业务/结构） × 模块 × 动作 × 数据范围（全部/仅自己/未来更多）"为逻辑主轴，前后端双重围栏。
- **迁移自足幂等**：baseline 唯一 AutoMigrate；列级迁移裸 SQL 自足幂等。

# 开发与部署
- 本项目最注重的就是底层架构的优美、统一和复用。
- 开发测试时分别构建后端和前端服务，不需要用到build.sh。