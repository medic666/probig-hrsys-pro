# 企业人事与行政管理系统 完整版企划书
## 一、项目概述
### 1.1 项目定位
本系统是面向中小企业的轻量生产级人事与行政管理系统，以**事件驱动为核心架构**，支持人员、公司、假勤、薪资、文件、审计全流程管理，主打单文件部署、结构清晰、鲁棒性强、依赖极简。
### 1.2 核心设计理念
- **事件驱动**：人员职务信息完全由职务事件链重建，所有变动留痕可追溯
- **审计全覆盖**：所有写操作全程留痕，审计日志不可删除，保证数据合规可追溯
- **容错性设计**：全局软删除机制，误删数据可一键恢复，操作风险极低
- **开箱即用**：单文件部署，启动自动初始化默认配置和管理员账号，零配置即可运行
- **权限可控**：预留完整RBAC权限体系，权限控制到模块级读写删导出，前端后端双重校验

## 二、技术栈选型
### 2.1 后端技术栈
- 核心框架：Go + Gin
- ORM：GORM
- 数据库：SQLite（单文件存储，无需额外部署）
- 认证：golang-jwt + bcrypt 密码加密
- 静态资源嵌入：Go embed（前端打包后嵌入二进制，实现单文件部署）
- 依赖原则：极简依赖，仅引入必要组件，避免过重框架
### 2.2 前端技术栈
- 构建工具：Bun + Vite
- 核心框架：Vue3 + TypeScript
- UI组件库：Element Plus
- 路由模式：Hash模式（适配单文件部署，无需后端fallback配置）
- 交互原则：直观轻量，所有业务对象展示名称不展示ID，不暴露数据库结构

## 三、数据模型设计
整体采用分层数据架构：注册层 → 事件层 → 关联层 → 快照层 → 汇总层 → 审计层 + 系统配置层 + 权限层
---
### 3.1 注册层：基础主数据
#### 3.1.1 人员主表（persons）
存储人员基础身份信息，全局软删除
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uint | 主键 | 系统内部ID，前端不展示 |
| name | varchar(64) | 非空 | 人员姓名 |
| id_card | varchar(32) | 唯一索引、加密存储 | 身份证号，全局唯一 |
| gender | tinyint | | 性别 |
| birthday | date | | 生日 |
| nation | varchar(32) | | 民族 |
| native_place | varchar(128) | | 籍贯 |
| address | varchar(256) | | 住址 |
| political_status | varchar(32) | | 政治面貌 |
| marital_status | tinyint | | 婚姻状态 |
| alias | varchar(64) | | 别名 |
| created_at/updated_at/deleted_at | datetime | | GORM标准时间字段，软删除标记 |
> 唯一约束：身份证号全局唯一；手机号、邮箱不做全局唯一限制

#### 3.1.2 人员关联子表（一对多，级联软删除）
##### 人员电话表（person_phones）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID，外键 |
| phone | varchar(32) | 加密存储 |
| remark | varchar(64) | 备注（如工作号、私人号） |

##### 人员邮箱表（person_emails）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| email | varchar(128) | 加密存储 |
| remark | varchar(64) | 备注 |

##### 人员银行卡表（person_bank_cards）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| bank_card | varchar(64) | 银行卡号，加密存储 |
| bank_name | varchar(64) | 开户行 |
| remark | varchar(64) | 备注 |

#### 3.1.3 公司主表（companies）
存储公司基础信息，全局软删除
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uint | 主键 | 系统内部ID |
| name | varchar(128) | 非空 | 公司名称 |
| credit_code | varchar(64) | 唯一索引 | 统一社会信用代码，全局唯一 |
| address | varchar(256) | | 地址 |
| contact_phone | varchar(32) | | 联系电话 |
| bank_name | varchar(64) | | 开户行 |
| bank_account | varchar(64) | 加密存储 | 银行账号 |
| created_at/updated_at/deleted_at | datetime | | 软删除标记 |

#### 3.1.4 文件主表（files）
所有上传文件统一存储，文件内容以BLOB形式存入SQLite，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| name | varchar(128) | 文件原始名称 |
| size | bigint | 文件大小（字节） |
| mime_type | varchar(64) | 文件类型 |
| content | blob | 文件二进制内容 |
| uploader_id | uint | 上传人ID |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

---
### 3.2 事件层：业务核心驱动
所有变动均通过事件记录，是快照、汇总的唯一数据来源
#### 3.2.1 职务事件表（position_events）
记录人员所有职务变动，仅存储发生变更的字段，未变更字段为NULL，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| event_name | varchar(128) | 事件名称（如入职、调薪、转正） |
| effective_date | date | 生效日期 |
| attendance_group | varchar(64) | 考勤组（仅作分类标记，无规则关联） |
| entry_date | date | 入职日期 |
| leave_date | date | 离职日期 |
| has_annual_leave | bool | 享有年假标识（外包人员关闭） |
| has_attendance_bonus | bool | 享有全勤奖标识（外包人员关闭） |
| base_salary | decimal(10,2) | 基本工资 |
| performance_salary | decimal(10,2) | 绩效工资基数 |
| salary_days | int | 计薪天数 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴月标准 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 核心规则：
> 1. 事件仅存储变更字段，未变更字段保持NULL
> 2. 同日多条事件按创建时间倒序叠加，后创建的覆盖先创建的同名字段
> 3. 增删改任意事件后，自动触发该员工全量职务快照重建，重建范围从最早生效事件到所有事件的最晚生效日期

#### 3.2.2 假勤事件表（attendance_events）
单表冗余设计，存储所有假勤明细事件，单日单条粒度，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| event_date | date | 事件日期（自然日） |
| event_type | varchar(32) | 事件类型：出勤/休假/加班/违纪/年假调整 |
| sub_type | varchar(32) | 子类型：<br>出勤：普通出勤、补班出勤<br>休假：调休、事假、病假、年假、法定假、福利假<br>加班：工作日加班、节假日加班<br>违纪：缺卡、迟到、早退<br>年假调整：年假配发、年假结转 |
| hours | decimal(4,1) | 事件时长（小时精度） |
| late_minutes | int | 违纪专用：迟到/早退分钟数 |
| leave_adjust_amount | decimal(4,1) | 年假调整专用：调整额度（天） |
| is_special_approval | bool | 是否特批（年假/调休额度不足时使用） |
| remark | varchar(256) | 备注 |
| batch_id | varchar(64) | 批次号（批量操作/导入用） |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 核心规则：
> 1. 小时精度，单日允许多条同类型事件，仅重复提示不强制拦截
> 2. 前端支持跨天录入，后端自动拆分为单日单条明细
> 3. 支持批量创建：按多选人员、按考勤组筛选批量生成相同事件

#### 3.2.3 工资事件表（salary_events）
工资调整事件，支持手动指定归属月份，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份（格式：YYYY-MM，手动指定） |
| event_type | varchar(32) | 事件类型：绩效调整、提成、奖惩、借款扣除、个税扣除、其他 |
| amount | decimal(10,2) | 金额/系数值：<br>绩效调整：最终绩效系数<br>其他：正负金额 |
| event_name | varchar(128) | 事件名称 |
| remark | varchar(256) | 备注 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 核心规则：
> 1. 同月多条绩效调整事件：取最新创建的系数值（覆盖式）
> 2. 同月其它工资事件：所有增量累加计算

---
### 3.3 关联层
#### 文件关联中间表（file_relations）
实现文件与业务对象的多对多关联，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| file_id | uint | 关联文件ID |
| target_type | varchar(32) | 关联对象类型：person/company/position_event/attendance_event/salary_event |
| target_id | uint | 关联对象ID |
| created_at/deleted_at | datetime | 软删除标记 |
> 级联规则：
> 1. 删除业务对象（人员/公司/事件）：仅软删除对应的关联记录，不删除文件本体
> 2. 删除文件本体：同步软删除所有该文件的关联记录
> 3. 恢复操作对称：恢复主数据同步恢复对应关联记录

---
### 3.4 时点快照层
#### 职务快照表（position_snapshots）
每日一条完整职务信息，由职务事件全量重建生成，全局软删除
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| snapshot_date | date | 快照日期 |
| attendance_group | varchar(64) | 考勤组 |
| entry_date | date | 入职日期 |
| leave_date | date | 离职日期 |
| has_annual_leave | bool | 享有年假标识 |
| has_attendance_bonus | bool | 享有全勤奖标识 |
| base_salary | decimal(10,2) | 基本工资 |
| performance_salary | decimal(10,2) | 绩效工资基数 |
| salary_days | int | 计薪天数 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴标准 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 重建规则：
> 1. 触发时机：职务事件增删改后自动触发
> 2. 重建范围：清空该员工所有快照，从最早生效事件重建到所有事件的最晚生效日期
> 3. 数据完整性：每日一条完整职务信息，无字段缺失

---
### 3.5 时段汇总层
#### 3.5.1 假勤汇总表（attendance_summaries）
月度假勤核算结果，手动核算生成
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份 |
| work_days | decimal(4,1) | 普通出勤天数 |
| make_up_days | decimal(4,1) | 补班出勤天数 |
| sick_leave_days | decimal(4,1) | 病假天数 |
| personal_leave_days | decimal(4,1) | 事假天数 |
| annual_leave_days | decimal(4,1) | 年假天数 |
| statutory_leave_days | decimal(4,1) | 法定假天数 |
| welfare_leave_days | decimal(4,1) | 福利假天数 |
| overtime_workday_hours | decimal(5,1) | 工作日加班时长 |
| overtime_holiday_hours | decimal(5,1) | 节假日加班时长 |
| violation_count | int | 违纪次数 |
| last_calc_at | datetime | 最后核算时间 |
| is_locked | bool | 是否锁定 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 规则：事件变动不自动更新汇总，仅手动核算时更新；锁定后禁止修改对应月份假勤事件

#### 3.5.2 工资汇总表（salary_summaries）
月度工资核算结果，手动核算生成
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份 |
| attendance_salary | decimal(10,2) | 出勤工资 |
| overtime_salary | decimal(10,2) | 加班工资 |
| attendance_bonus | decimal(10,2) | 全勤奖 |
| performance_salary | decimal(10,2) | 最终绩效工资 |
| total_allowance | decimal(10,2) | 补贴合计 |
| total_adjustment | decimal(10,2) | 调整项合计（提成+奖惩+借款+个税等） |
| total_deduction | decimal(10,2) | 代扣合计（社保+公积金） |
| final_salary | decimal(10,2) | 实发工资 |
| last_calc_at | datetime | 最后核算时间 |
| is_locked | bool | 是否锁定 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |
> 规则：
> 1. 事件变动不自动更新汇总，仅手动核算时更新
> 2. 幂等保证：同一人同一月份相同事件数据，多次核算结果完全一致
> 3. 锁定后禁止修改对应月份职务/假勤/工资事件

---
### 3.6 审计层
#### 审计日志表（audit_logs）
仅追加、不可删除，无软删除字段
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| operator_id | uint | 操作人ID |
| operator_name | varchar(64) | 操作人姓名（冗余存储，方便展示） |
| target_type | varchar(64) | 操作对象类型 |
| target_id | uint | 操作对象ID |
| action | varchar(32) | 操作类型：新增/修改/删除/恢复/核算/配置修改等 |
| before_snapshot | text | 操作前完整数据快照（JSON） |
| after_snapshot | text | 操作后完整数据快照（JSON） |
| batch_id | varchar(64) | 批次号（批量操作用） |
| ip | varchar(64) | 操作IP |
| created_at | datetime | 操作时间 |
> 覆盖范围：全量写操作审计，包括业务数据、系统配置、用户权限、角色调整等所有写操作
> 核心约束：仅支持查询，无任何修改/删除接口，永久留存

---
### 3.7 系统配置层
#### 系统配置表（sys_config）
键值对存储全局配置，服务启动时全量加载到内存缓存
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| config_key | varchar(64) | 配置键（模块.配置项格式，唯一索引） |
| config_value | text | 配置值（字符串存储） |
| config_name | varchar(128) | 配置显示名称 |
| config_desc | varchar(256) | 配置说明 |
| value_type | varchar(16) | 值类型：string/number/bool/select |
| option_values | text | 下拉可选值（JSON数组） |
| updated_at | datetime | 更新时间 |
> 核心可配置项清单：
> - 考勤类：调休管控模式开关、假期特批开关、最小请假单位
> - 薪资类：病假系数、工作日加班系数、节假日加班系数、全勤奖系数、高温补贴发放月份、默认年假额度
> - 通用类：分页默认条数、导出最大条数、文件上传大小限制

---
### 3.8 权限层（RBAC0模型）
#### 3.8.1 用户表（users）
系统登录账号，关联人员表
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| username | varchar(64) | 登录账号，唯一 |
| password_hash | varchar(128) | bcrypt加密密码 |
| person_id | uint | 关联人员ID |
| is_first_login | bool | 是否首次登录（强制改密） |
| status | tinyint | 账号状态：启用/禁用 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

#### 3.8.2 角色表（roles）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| name | varchar(64) | 角色名称 |
| remark | varchar(256) | 角色备注 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

#### 3.8.3 权限表（permissions）
模块级权限，粒度为「模块+操作」
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| permission_key | varchar(64) | 权限标识（模块.操作，如person.read） |
| name | varchar(64) | 权限名称 |
| module | varchar(32) | 所属模块 |
| action | varchar(32) | 操作类型：read/write/delete/export |

#### 3.8.4 关联表
- 用户角色关联表：多对多，一个用户可绑定多个角色
- 角色权限关联表：多对多，一个角色可绑定多个权限

## 四、核心业务逻辑
### 4.1 人员管理模块
#### 4.1.1 基础信息管理
- 支持人员基础信息的增删查改、搜索筛选、分页、Excel导出
- 多值字段（电话、邮箱、银行卡）独立管理，支持单独增删改
- 身份证号全局唯一校验，重复录入拦截
- 所有操作计入审计日志
#### 4.1.2 职务事件管理
- 操作路径：选择人员 → 填写事件名称 → 选择生效日期 → 勾选影响字段 → 填写字段值 → 提交
- 事件仅提交变更字段，未勾选字段保持NULL
- 事件提交后自动触发该员工全量职务快照重建
- 支持查看所有历史职务事件，支持修改、删除（修改/删除后自动重建快照）
#### 4.1.3 文件关联管理
- 展示人员关联的所有附件，支持上传新文件自动关联、解除关联
- 解除关联仅删除关联记录，不删除文件本体
#### 4.1.4 删除与恢复
- 删除人员：级联软删除所有关联数据（职务/假勤/工资事件、子表、文件关联）
- 恢复人员：同步恢复所有关联数据，恢复操作计入审计

### 4.2 公司管理模块
- 公司基础信息增删查改、搜索筛选、导出
- 统一社会信用代码全局唯一校验
- 公司附件关联管理，规则同人员附件
- 级联软删除/恢复，逻辑同人员管理

### 4.3 假勤管理模块
#### 4.3.1 假勤事件管理
- 支持按人员、时间、类型多维度筛选查询，分页、导出
- 单条创建：选择人员、日期、类型、时长、备注提交
- 跨天创建：选择起止日期，后端自动拆分为单日单条事件
- 批量创建：支持多选人员、按考勤组筛选批量生成相同假勤事件，批量操作每条事件独立审计，带统一批次号
- 同日同类型事件重复仅提示不强制拦截，适配补录调账场景
#### 4.3.2 年假规则
- 资格管控：职务快照中「享有年假」标识为关闭的员工，禁止提交年假申请
- 额度计算：实时动态计算，额度=累计配发 + 累计结转 - 累计已休
- 提交校验：默认余额不足拦截，支持特批标记绕过校验
- 周年配发：提供「年假周年批量处理」功能，手动触发后自动检查所有入职周年到期的在职员工，生成新年假配发事件
- 年假结转：支持手动录入结转事件，批量处理可扩展自动结转规则
#### 4.3.3 调休规则
- 管控模式：系统配置开关，可切换管控/无管控模式
- 管控模式：加班事件自动累计调休额度，提交调休校验余额，支持特批
- 无管控模式：调休无需校验额度，直接录入
- 有效期：默认永久有效，预留配置扩展位
#### 4.3.4 考勤核算
- 支持按月份、人员范围筛选核算
- 核算逻辑：按月聚合所有假勤事件生成汇总表，与工资核算独立无依赖
- 过期提示：汇总列表动态对比事件最后更新时间与核算时间，事件变动后提示「数据已变动，请重新核算」
- 月度锁定：核算确认后可锁定当月，锁定后禁止增删改对应月份假勤事件，解锁操作计入审计
#### 4.3.5 钉钉导入预留框架
- 完整流程框架：文件上传 → 解析占位 → 结果预览 → 确认入库 → 错误提示
- 仅文件内容解析逻辑留空，后续补充解析函数即可完整使用
- 导入事件与手动录入事件规则完全一致，自动拆分、审计记录

### 4.4 工资管理模块
#### 4.4.1 工资事件管理
- 支持按人员、月份、类型筛选查询，分页、导出
- 事件支持手动指定归属月份，适配补录历史调整场景
- 绩效调整事件：同月多条取最新值（覆盖式）
- 其它工资事件：同月多条增量累加
#### 4.4.2 薪资核算规则
**基础公式**：
1. 日薪 = 基本工资 / 计薪天数
2. 在职判断：仅核算入职日期后、离职日期前的日期，入职离职当月按实际在职天数折算
3. 出勤工资 =（普通出勤天数 + 补班出勤天数 + 病假天数×0.6 + 年假/法定假/福利假天数）× 日薪
4. 全勤奖：
   - 前置校验：当月有事假记录 → 全勤奖直接为0
   - 无事假：（出勤计薪天数 - 违纪次数）× 全勤奖系数
5. 加班工资：
   - 基数 = 基本工资 + 餐补
   - 工作日加班工资 = 基数 / 计薪天数 / 8 × 工作日加班小时数 × 1.5
   - 节假日加班工资 = 基数 / 计薪天数 / 8 × 节假日加班小时数 × 2
6. 绩效工资 = 绩效工资基数 × 当月绩效系数（无绩效事件默认1，绩效事件取最新值）
7. 补贴合计：
   - 房补、职位津贴、交通补贴、保险补偿、公积金补偿：全额按月发放
   - 高温补贴：当月属于配置的发放月份则全额发放，否则为0
8. 调整项合计：所有提成、奖惩、借款扣除、个税扣除等金额类事件累加
9. 代扣合计：社保代扣 + 公积金代扣（取职务快照当月标准）
10. 实发工资 = 出勤工资 + 加班工资 + 全勤奖 + 绩效工资 + 补贴合计 + 调整项合计 - 代扣合计
#### 4.4.3 工资核算
- 支持按月份、人员范围筛选核算
- 直接读取假勤事件明细+职务快照计算，不依赖假勤汇总结果
- 幂等保证：相同输入数据多次核算结果完全一致
- 过期提示、月度锁定规则同假勤汇总
#### 4.4.4 全勤奖资格
- 职务快照中「享有全勤奖」标识为关闭的员工，全勤奖直接为0，不进入后续计算

### 4.5 文件管理模块
- 全局文件列表展示，支持按名称、类型、上传时间筛选
- 文件上传：BLOB存入SQLite，自动生成文件记录
- 关联调整：支持修改文件的关联对象，关联多业务实体
- 文件删除：软删除文件本体，同步删除所有关联记录
- 文件恢复：恢复文件本体，同步恢复所有关联记录
- 所有操作计入审计

### 4.6 操作审计模块
- 全量操作日志展示，支持按操作人、操作类型、对象类型、时间范围筛选
- 每条日志展示操作人、操作时间、操作类型、前后完整快照
- 支持按批次号筛选批量操作记录
- 仅支持查询和导出，无任何修改、删除接口，数据永久留存

### 4.7 用户与权限管理
- 用户管理：用户增删改、重置密码、启用禁用、绑定人员、分配角色
- 角色管理：角色增删改、权限分配，支持自定义角色
- 权限粒度：模块级读/写/删/导出四级权限，覆盖所有业务模块
- 前端控制：根据权限动态渲染菜单和操作按钮，无权限的功能不可见
- 后端控制：所有接口统一权限校验，前端隐藏不绕过权限校验
- 默认初始化：首次启动自动创建超级管理员账号（admin/admin123），首次登录强制修改密码

### 4.8 系统配置模块
- 分组展示所有全局配置项，按值类型自动渲染表单控件
- 配置修改自动刷新内存缓存，实时生效
- 所有配置修改操作计入审计日志
- 支持配置项扩展，新增配置仅需插入初始数据即可

## 五、后端架构设计
### 5.1 分层架构
1. **路由层**：Gin路由注册，统一前缀分组
2. **中间件层**：JWT认证中间件、权限校验中间件、审计拦截中间件、全局错误处理中间件、CORS中间件
3. **业务层（Service）**：核心业务逻辑实现，事务控制所有写操作
4. **数据层（DAO）**：GORM数据操作，软删除、敏感字段自动加解密
### 5.2 核心特性
- **统一响应格式**：所有接口返回标准结构 `{code, msg, data}`，错误码统一规范
- **全局错误处理**：统一捕获异常，返回友好错误提示，不暴露内部错误细节
- **事务保证**：所有多表写操作全部包裹事务，保证数据一致性
- **全局软删除**：所有业务表默认开启GORM软删除，查询自动过滤已删除数据
- **敏感字段加密**：身份证号、银行卡号、手机号等敏感字段自动加解密，密钥通过环境变量配置
- **配置热加载**：配置修改后自动刷新内存缓存，无需重启服务
- **审计全拦截**：全局拦截所有写操作，自动生成审计日志，业务代码无感知

## 六、前端架构设计
### 6.1 核心规范
- 路由：Hash模式，适配单文件部署
- 状态管理：Pinia管理用户信息、权限、全局配置
- 请求封装：统一Axios拦截器，自动携带Token、统一错误处理、权限拦截
### 6.2 交互规范
- **无ID展示**：所有业务对象展示名称，任何页面不暴露数据库ID、表名、字段名
- **统一列表页**：所有模块列表页统一布局：顶部搜索筛选区 + 操作按钮区 + 表格区 + 分页区
- **动态权限**：菜单、按钮根据当前用户权限动态显示，无权限则隐藏
- **表单规范**：新增/编辑统一弹窗表单，表单校验前后端双重保证
- **操作反馈**：所有操作提供明确的成功/失败提示，危险操作二次确认

## 七、部署与运行
### 7.1 单文件部署
- 前端打包后通过Go embed嵌入Go二进制，整个服务仅一个可执行文件
- SQLite数据库文件默认生成在可执行文件同级目录，支持通过环境变量自定义路径
### 7.2 环境变量配置
所有核心配置支持环境变量覆盖，无需配置文件：
- `DB_PATH`：数据库文件路径（默认./hr.db）
- `JWT_SECRET`：JWT签名密钥（默认随机生成）
- `ENCRYPT_KEY`：敏感字段加密密钥（默认随机生成）
- `SERVER_PORT`：服务端口（默认8080）
### 7.3 初始化流程
- 首次启动自动创建所有表结构
- 自动插入默认系统配置
- 自动创建默认超级管理员账号admin/admin123
- 开箱即用，启动后直接访问即可登录
### 7.4 数据备份
- 所有数据（含文件）全部存储在单个SQLite文件中
- 备份直接拷贝数据库文件即可，恢复时替换文件即可

## 八、扩展预留设计
1. **钉钉打卡导入**：完整流程框架已预留，仅需补充文件解析逻辑即可上线
2. **角色权限扩展**：RBAC框架完整，后续新增财务、老板等角色直接在页面配置即可，无需改代码
3. **薪资规则扩展**：所有系数类规则已接入系统配置，调整无需改代码
4. **数据权限扩展**：预留数据权限扩展位，后续可实现不同角色看不同范围人员数据
5. **定时任务扩展**：预留定时器接入位，后续可扩展自动年假结转、自动核算等定时功能