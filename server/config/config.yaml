server:
  port: 8080
  mode: debug

database:
  type: sqlite
  path: ./data/probig.db

file_storage:
  path: ./data/uploads

jwt:
  secret: probig-jwt-secret-change-in-production

log:
  level: info
  file: ./data/probig.log
