# 开发与部署指南

## 项目结构

```
probig/
├── web/                  # 前端 (Vue3 + Element Plus)
│   ├── src/
│   │   ├── api/          # 后端接口封装
│   │   ├── components/   # 通用组件 (ProTable, ProFormDialog 等)
│   │   ├── views/        # 业务页面
│   │   ├── router/       # 路由 (Hash 模式)
│   │   ├── stores/       # Pinia 状态 (user, permission, config)
│   │   ├── utils/        # 工具函数 (request 拦截器, 日期, 金额)
│   │   └── styles/       # 全局样式
│   ├── package.json
│   └── vite.config.ts
├── server/               # 后端 (Go + Gin + GORM + SQLite)
│   ├── cmd/server/       # 入口 + 静态资源嵌入
│   ├── config/           # 开发配置文件
│   │   └── config.yaml
│   ├── internal/
│   │   ├── config/       # 配置加载与路径解析
│   │   ├── dao/          # 数据库操作 + 审计 Hook
│   │   ├── handler/      # HTTP 请求处理
│   │   ├── middleware/    # JWT 认证 + 权限校验
│   │   ├── model/        # 数据模型 (35+ 表)
│   │   ├── router/       # 路由注册
│   │   ├── service/      # 核心业务逻辑 (L0→L3 投影)
│   │   └── utils/        # 工具函数 (JWT, 加密, DateOnly)
│   ├── go.mod
│   └── go.sum
├── dist/                 # 单文件部署交付区
│   └── probig-server     # 编译产物，首次运行自动生成配置和数据
├── build.sh              # 一键构建脚本
└── examples/             # 示例数据文件
```

## 开发模式

### 启动后端

```bash
cd server
go run ./cmd/server/
```

- 自动加载 `server/config/config.yaml`（`mode: debug`，路由日志可见）
- 数据库和上传文件落在 `server/data/` 下
- 监听 `localhost:8080`

### 启动前端

```bash
cd web
bun install     # 首次
bun run dev
```

- 监听 `localhost:5173`
- 自动代理 `/api` 请求到 `localhost:8080`
- 热更新生效

## 生产构建

```bash
./build.sh
```

- 自动构建前端（Vite 输出到 `server/cmd/server/static/`）
- 编译 Go 二进制（前端静态资源嵌入二进制内）
- 单二进制输出到 `dist/probig-server`

## 部署

将发行版的 `dist/` 目录拷贝到目标机器：

```
dist/
└── probig-server    # 43MB 单二进制文件
```

```bash
cd dist
./probig-server
```

**首次启动：**
- 无 `config.yaml` → 自动在二进制同级目录生成默认配置
- 无数据库 → 自动初始化 SQLite 并创建默认管理员账号
- 所有持久化数据落在 `dist/` 同级目录：

```
dist/
├── probig-server
├── config.yaml          # 首次启动自动生成
└── data/                # 首次启动自动创建
    ├── probig.db        # SQLite 数据库
    ├── probig.log       # 运行日志
    └── uploads/         # 用户上传文件
```

**二次启动：** 自动读取已有配置和数据库，无需任何额外操作。

## 配置说明

`config.yaml`（首次启动自动生成，可手动编辑）：

```yaml
server:
  port: 8080          # 监听端口
  mode: release       # debug 或 release

database:
  type: sqlite
  path: ./data/probig.db

file_storage:
  path: ./data/uploads

jwt:
  secret: probig-jwt-secret-change-in-production

log:
  level: info
  file: ./data/probig.log
```

所有相对路径（`./data/...`）在部署时自动解析到二进制所在目录。

**删除 `config.yaml` 再重启 = 用默认值重新生成一套干净的配置。**

## 默认账号

| 用户名 | 密码 | 说明 |
|--------|------|------|
| admin | admin123 | 超级管理员，首次登录强制改密 |

## 常用命令

```bash
go run ./cmd/server/          # 开发启动后端
bun run dev                   # 开发启动前端
./build.sh                    # 生产构建
go test ./internal/...        # 运行后端测试
```
