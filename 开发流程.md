### 开发执行顺序
本开发流程采用前后端并行推进、分阶段闭环验收的模式，每个阶段完成后必须通过对应测试验收目标，方可进入下一阶段。

#### 阶段一：工程初始化与开发规范对齐
**后端开发任务**
1. 初始化Go + Gin项目骨架，完成依赖引入与目录结构规划
2. 完成GORM与SQLite集成，实现数据库自动连接与表结构迁移基础能力
3. 落地代码规范、目录分层规范，完成基础配置文件管理

**前端开发任务**
1. 初始化Vue3 + TypeScript + Vite项目，完成Bun包管理配置
2. 引入Element Plus、Pinia、Vue Router、Axios、dayjs等核心依赖
3. 落地ESLint、Prettier代码规范，配置全局样式变量与基础重置样式
4. 配置Hash路由模式与相对路径静态资源，适配单文件部署

**测试验收目标**
1. 前后端项目可独立正常启动运行
2. 代码规范校验全量通过
3. 后端可正常连接SQLite并执行基础建表操作
4. 前端可正常访问基础路由页面

#### 阶段二：底层基础能力建设
**后端开发任务**
1. 实现统一响应结构体、全局异常捕获中间件、统一分页工具封装
2. 实现软删除通用封装、事务工具封装、审计日志基础切面能力
3. 完成bcrypt密码加密、JWT签发与校验工具类开发
4. 实现系统配置内存缓存机制，支持启动加载与热更新
5. 完成基础工具函数库建设，包含日期处理、数值计算、参数校验等

**前端开发任务**
1. 完成Axios请求封装，实现请求拦截、响应拦截、错误统一处理
2. 完成Pinia全局状态框架搭建，实现user、permission、config三类store基础结构
3. 完成Vue Router基础路由框架配置，实现嵌套路由结构
4. 实现全局工具函数库，包含日期格式化、考勤单位换算、金额格式化、权限判断基础方法

**测试验收目标**
1. 后端接口统一返回格式符合规范，异常场景可被全局捕获并返回友好提示
2. 后端基础工具类单元测试通过率100%
3. 前端可正常发起接口请求，自动处理Token注入与异常响应
4. 前端全局工具函数功能验证通过，路由跳转正常

#### 阶段三：前端全局布局与通用组件开发
**后端开发任务**
1. 提供基础测试接口，配合前端完成组件联调验证
2. 完善通用分页、筛选查询的基础DAO封装

**前端开发任务**
1. 实现全局三栏Layout布局，包含可折叠侧边栏、顶部导航栏、主内容区
2. 开发ProTable通用列表组件，集成搜索筛选区、操作按钮区、数据表格区、底部分页区
3. 开发ProFormDialog通用弹窗表单组件，支持新增/编辑双模式、表单校验、提交加载
4. 开发NameSelect名称选择器组件，实现本地模糊匹配与远程搜索双模式
5. 实现统一状态标签组件、空状态组件、加载态组件、回收站通用抽屉组件

**测试验收目标**
1. 全局布局适配正常，菜单折叠、路由跳转交互符合规范
2. 三类核心通用组件全功能验证通过，可直接复用于后续业务页面
3. 通用组件交互逻辑与设计规范完全一致，无自定义交互逻辑
4. 回收站抽屉通用能力验证通过

#### 阶段四：认证与权限体系全链路闭环
**后端开发任务**
1. 完成用户、角色、权限及关联表的数据模型与CRUD接口开发
2. 实现JWT鉴权中间件、接口级权限校验中间件
3. 开发登录、登出、修改密码、获取用户信息与权限列表接口
4. 实现首次登录强制改密逻辑、默认超级管理员账号初始化逻辑
5. 实现账号启用禁用控制、角色权限分配逻辑

**前端开发任务**
1. 开发登录页面，实现账号密码登录、表单校验、错误提示
2. 实现Token本地持久化、路由守卫鉴权、无权限跳转逻辑
3. 实现v-permission权限指令与hasPermission全局方法，完成按钮级权限控制
4. 实现动态侧边栏菜单，根据权限列表自动过滤展示
5. 开发首次登录强制改密弹窗、403/404错误页面
6. 开发用户管理、角色管理、权限分配页面

**测试验收目标**
1. 登录流程全链路贯通，账号密码校验准确，Token签发与鉴权生效
2. 角色权限分配后，菜单级、按钮级、接口级权限三重校验同步生效
3. 无权限接口访问被后端拦截，前端无对应操作入口
4. 首次登录强制改密逻辑验证通过，默认管理员账号可正常登录并改密
5. 账号禁用后无法登录，权限修改后实时生效

#### 阶段五：核心主数据模块开发
**后端开发任务**
1. 完成人员主表、人员关联子表（电话、邮箱、银行卡）的数据模型与CRUD接口开发
2. 完成公司主表的数据模型与CRUD接口开发
3. 落地人员身份证号、公司统一社会信用代码的部分唯一索引约束
4. 实现软删除、恢复逻辑，级联处理关联子表与关联数据
5. 实现列表搜索、筛选、分页、Excel导出能力
6. 完成文件关联基础接口开发，支持业务对象与文件的绑定与解绑

**前端开发任务**
1. 开发人员管理列表页、新增/编辑弹窗、详情弹窗（基础信息标签页）
2. 实现人员多值字段（电话、邮箱、银行卡）的独立管理能力
3. 开发公司管理列表页、新增/编辑弹窗、详情弹窗
4. 实现两个模块的搜索筛选、分页、回收站、导出功能
5. 实现人员详情、公司详情的附件关联管理能力

**测试验收目标**
1. 人员、公司的增删改查功能全量验证通过
2. 未删除记录的身份证号、统一社会信用代码唯一约束生效，已删除记录不占用唯一索引
3. 软删除与恢复功能正常，关联数据级联处理正确
4. 列表搜索、筛选、分页、导出功能符合业务要求
5. 文件关联与解绑功能正常，详情页附件展示准确

#### 阶段六：职务事件与基础投影链路开发
**后端开发任务**
1. 完成职务事件表的数据模型与CRUD接口开发，实现单人员内事件序列号生成机制
2. 开发职务时段快照全量重建逻辑，实现事件按生效日期、序列号排序叠加规则
3. 实现人员当前职务信息读取接口、职务变动历史查询接口
4. 实现事件增删改后自动触发投影重建的联动机制
5. 落地投影last_calc_at标记与数据过期判断逻辑

**前端开发任务**
1. 开发职务事件管理列表页，支持按人员、日期范围、事件名称筛选
2. 开发职务事件新增/编辑弹窗，实现复选框驱动的动态表单
3. 人员详情页新增职务信息标签页，展示当前职务与变动历史
4. 接入统一投影状态标签，展示数据最新状态

**测试验收目标**
1. 职务事件增删改后自动触发快照重建，重建结果幂等，多次执行结果完全一致
2. 同日多条事件按序列号升序叠加规则验证正确，字段覆盖逻辑符合设计
3. 职务时段连续无重叠、无空窗，离职事件处理逻辑正确
4. 人员当前职务信息读取准确，变动历史完整可追溯
5. 事件软删除、恢复后投影自动同步更新

#### 阶段七：考勤全链路开发
**后端开发任务**
1. 完成考勤事件表的数据模型与CRUD接口开发，实现单人员内事件序列号生成机制
2. 开发日考勤记工时投影重算逻辑，实现多类型考勤事件的聚合计算规则
3. 开发月度考勤核算逻辑，实现职务快照加权计算、出勤工资、加班工资、全勤奖核算规则
4. 实现考勤事件增删改后自动触发日记工时重算的联动机制
5. 实现数据变动标记逻辑，下层数据更新后自动标记上层投影过期
6. 完成批量考勤事件录入接口、跨天事件自动拆分逻辑
7. 预留钉钉导入流程框架接口

**前端开发任务**
1. 开发考勤事件管理页，实现单条新增、批量新增、筛选、导出功能
2. 实现事件类型与子类型两级联动、批量新增两步式交互
3. 开发日考勤记工时查询页，支持按人员、日期范围筛选，支持查看原始事件明细
4. 开发月度考勤核算页，支持按月份、人员范围核算，展示核算状态与结果
5. 实现考勤数据下钻追溯能力，从月度核算到日明细再到原始事件

**测试验收目标**
1. 考勤事件增删改后，对应日期日记工时自动重算，计算结果准确
2. 批量新增事件自动按日拆分，序列号分配正确，无重复无跳号
3. 月度考勤核算公式验证准确，加权工资、加班工资、全勤奖计算符合规则
4. 下层数据变动后，上层投影自动标记为数据已变动状态
5. 全链路追溯功能正常，核算结果可逐层定位到原始事件
6. 导出功能数据准确，变动数据导出二次确认逻辑生效

#### 阶段八：假期权益全链路开发
**后端开发任务**
1. 完成年假权益事件表的数据模型与CRUD接口开发，实现序列号生成机制
2. 开发年假余额时段快照投影重建逻辑，联动年假权益事件与考勤休假事件
3. 开发调休余额时段快照投影重建逻辑，联动补班与调休考勤事件
4. 实现年假周年批量结转逻辑，包含人员筛选、批次生成、事件生成、余额更新全流程
5. 实现批次反结账能力，支持冲销系统周期事件并回滚余额
6. 完成通用批次管理表的业务逻辑封装

**前端开发任务**
1. 开发年假事件流水页、年假余额查询页，支持额度构成明细下钻
2. 开发年假周年结转管理页，支持批量结转、反结账、批次明细查看
3. 开发调休事件流水页、调休余额查询页，支持额度构成明细下钻
4. 考勤事件录入年假、调休类型时，自动展示当前人员可用余额

**测试验收目标**
1. 年假余额计算准确，权益事件与休假事件联动同步，投影重算幂等
2. 调休余额计算准确，补班与调休事件联动同步，投影重算幂等
3. 周年批量结转逻辑正确，符合周期规则与结转规则，批次记录完整
4. 反结账操作后，事件与余额准确回滚至操作前状态，支持重复结转幂等执行
5. 余额明细构成数据准确，与原始事件一一对应

#### 阶段九：薪资核算全链路开发
**后端开发任务**
1. 完成工资调整事件表的数据模型与CRUD接口开发，实现序列号生成机制
2. 开发月度工资总表核算逻辑，聚合考勤核算、职务快照、年假事件、工资事件四类数据
3. 实现工资核算历史版本管理，每次核算生成独立版本记录，同步更新主表最新数据
4. 实现全链路数据追溯接口，支持从工资汇总下钻至各明细项与原始事件
5. 实现多维度数据变动检测逻辑，下层数据更新后自动标记工资汇总过期
6. 完成工资数据导出接口开发

**前端开发任务**
1. 开发工资事件管理页，支持按人员、月份、事件类型筛选与管理
2. 开发月度工资汇总页，支持批量核算、状态展示、导出功能
3. 实现工资版本历史查看、相邻版本差异对比功能
4. 实现薪资构成明细下钻能力，支持逐层追溯至原始业务事件
5. 接入数据变动状态提示，导出时触发二次确认校验

**测试验收目标**
1. 薪资核算全公式验证准确，各项薪资构成计算符合业务规则
2. 每次核算生成独立版本记录，主表数据同步更新，版本号递增正确
3. 版本对比功能正常，可高亮展示变动项与变动金额
4. 四类下层数据任意变动后，工资汇总状态自动标记为数据已变动
5. 全链路追溯完整，所有薪资项均可定位至原始数据来源
6. 前置校验逻辑生效，未完成考勤核算的人员自动跳过并提示

#### 阶段十：配套支撑模块开发
**后端开发任务**
1. 完成文件管理全量接口开发，包含上传、下载、列表查询、关联调整、软删除与恢复
2. 完善审计日志全量记录能力，覆盖所有写操作与批量操作，保证前后快照完整
3. 完成系统配置管理接口开发，实现配置修改后内存缓存实时刷新
4. 全模块补全权限校验，覆盖所有读、写、删、导出操作

**前端开发任务**
1. 开发全局文件管理页，支持文件上传、关联调整、下载、删除、回收站管理
2. 开发审计日志列表页、详情弹窗，实现操作前后快照左右对比展示
3. 开发系统配置页面，按模块分组展示，根据值类型自动渲染表单控件
4. 全模块补全权限控制，无权限操作入口自动隐藏

**测试验收目标**
1. 文件上传、下载、多对象关联功能正常，软删除与恢复逻辑正确
2. 所有业务写操作均生成审计日志，前后快照数据完整准确
3. 系统配置修改后实时生效，无需重启服务
4. 全模块权限控制覆盖完整，前后端校验一致
5. 审计日志仅支持查询与导出，无修改删除入口

#### 阶段十一：全量集成联调与功能验收
**后端工作任务**
1. 全业务流程端到端联调，修复接口联调问题
2. 边界场景、异常场景逻辑补全与验证
3. 性能基础优化，保证常规数据量下接口响应达标
4. 配合完成全量功能验收测试

**前端工作任务**
1. 全模块页面联调，修复交互与数据展示问题
2. 补全所有异常场景、空状态、加载态的交互表现
3. 统一全模块交互规范与视觉样式
4. 配合完成全量功能验收测试

**测试验收目标**
1. 核心业务主流程（人员入职→职务配置→考勤录入→假期管理→薪资核算）全链路跑通，数据流转正确
2. 所有边界场景计算结果符合预期，异常输入被正确拦截并提示
3. 全模块软删除、恢复、导出、回收站功能验证通过
4. 权限体系全量验证，不同角色账号操作范围符合权限配置
5. 审计日志覆盖所有写操作，追溯链路完整合规
6. 所有投影层重算结果幂等，多次执行结果完全一致

#### 阶段十二：生产构建与交付验收
**后端工作任务**
1. 完成生产环境编译构建，嵌入前端静态资源至二进制文件
2. 验证单文件部署流程，验证初始化流程与默认账号可用性
3. 输出数据备份与恢复操作说明
4. 完成生产环境基础功能冒烟测试

**前端工作任务**
1. 完成生产环境构建优化，压缩包体积
2. 验证Hash路由、相对路径静态资源在生产环境的兼容性
3. 验证生产构建包与后端嵌入的一致性

**测试验收目标**
1. 单个二进制可执行文件可直接启动运行，无需额外依赖
2. 首次启动自动完成表结构迁移、默认配置插入、管理员账号创建
3. 生产环境全功能验证通过，与开发环境表现一致
4. 数据备份与恢复方案验证有效
5. 交付物符合单文件部署要求，可直接投入生产使用