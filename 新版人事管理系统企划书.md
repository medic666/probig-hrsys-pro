# 企业人事与行政管理系统企划书

## 一、项目概述

### 1.1 项目定位

面向中小企业的轻量生产级人事与行政管理系统，覆盖人员、公司、考勤、假期权益、薪资核算、文件、审计、权限全流程管理。单二进制部署，开箱即用。

### 1.2 核心设计理念

- **事件驱动**：全部业务事实以原始事件存储，结果数据可由事件全量重建，可追溯、可复现。
- **多层投影**：事件源 → 基础投影 → 业务核算 → 结果汇总四级分层，上层仅依赖下层，投影不可直接修改。
- **审计全覆盖**：操作全程留痕，审计日志仅追加不可删除；操作人/操作 IP 随请求上下文贯穿，审计随业务事务提交，保证记录与事实一致。
- **容错设计**：全局软删除，删除与恢复级联原子化；投影自动过滤已删除事件源。
- **开箱即用**：首次启动自动生成配置、数据库、默认管理员与权限体系。
- **权限可控**：RBAC 模块级读/写/删/导出四级权限，前端隐藏、后端校验、接口缓存三重保障。

## 二、系统架构

### 2.1 分层模型

| 层级 | 内容 | 触发方式 |
|------|------|----------|
| L0 事件源 | 职务事件、考勤事件、年假权益事件、工资调整事件 | 手动录入/导入/系统周期操作 |
| L1 基础投影 | 职务时段快照、日考勤记工时、年假余额、调休余额 | 事件变动自动重算 |
| L2 业务核算 | 月度考勤核算 | 手动触发，支持批量 |
| L3 结果汇总 | 月度工资总表 + 版本历史 | 手动触发，支持批量 |

### 2.2 架构规范

- 统一响应 `{code, msg, data}`；错误统一友好提示，不暴露内部细节。
- 全部多表写操作包裹事务；投影重建采用「生成新数据 → 事务替换」模式。
- 投影统一包含 `last_calc_at`（秒级精度），以下层最大时间戳判定过期，标记「数据已变动」。
- 事件源统一 `(person_id, seq)` 单调递增序列号，部分唯一索引保证删除/新建/恢复互不冲突。
- 审计由 GORM 回调自动拦截单表 CRUD，批量与跨表操作在 Service 层显式写入；批量按实体拆分为单条审计。
- 系统配置内存缓存热加载，修改实时生效，前端按元数据驱动渲染。
- 权限查询内存缓存（TTL + 主动失效双保险），角色/用户分配变更即时生效。

## 三、技术栈

| 分类 | 选型 |
|------|------|
| 后端 | Go + Gin + GORM + SQLite（纯 Go 驱动） |
| 认证 | golang-jwt（含随机 jti）+ bcrypt |
| 报表 | excelize（统一导出工具） |
| 前端 | Vue3 + TypeScript + Element Plus + Pinia + Axios + dayjs |
| 构建 | Vite + Bun，Hash 路由，相对路径静态资源 |
| 部署 | 前端经 go:embed 嵌入二进制，单文件交付 |

## 四、前端架构设计

### 4.1 总体架构

模块与后端一一对应，业务页面统一复用通用组件体系；Hash 路由 + 相对路径静态资源适配单文件部署；状态管理仅承载用户、权限、配置三类全局状态。

### 4.2 请求封装

- 统一拦截器：`code=0` 直返数据，非零弹出错误提示；401 清除会话跳转登录，403 提示无权限。
- 文件类响应（blob）自动解析后端文件名，统一经下载工具落盘。
- 后端错误细节绝不透出，仅展示友好业务提示。

### 4.3 路由与权限

- 路由 meta 携带标题与权限标识；前端全量定义路由，菜单按权限过滤，路由守卫做访问兜底。
- 权限三级控制：菜单过滤、`v-permission` 按钮级移除、后端接口独立校验。
- 刷新后重新拉取用户与权限，不信任本地缓存。

### 4.4 通用组件体系

| 组件 | 职责 |
|------|------|
| ProTable | 搜索/操作/批量/分页一体化列表，SearchField 类型系统（input/select/date-range/month-range/name-select/person-select/month） |
| ProFormDialog | 新增/编辑双模式弹窗，字段类型（input/number/select/date/textarea/switch/person-select） |
| NameSelect | 名称选择器，v-model 绑定 ID、展示业务名；500 条阈值自动切换远程搜索 |
| RecycleBinDrawer | 回收站抽屉，统一恢复交互与二次确认 |
| FileAttachPanel | 业务对象附件面板（上传/关联/下载/移除） |
| StatusTag | 投影状态三态标签（未核算/已核算/数据已变动） |
| ChangePasswordDialog | 常规/强制双模式改密 |

### 4.5 交互与展示规范

- 搜索按钮触发查询、重置清空并自动查询；分页参数统一 `pageNum/pageSize`，响应统一 `{list, total}`。
- 危险操作（删除/恢复/覆盖核算/反结账）一律二次确认；加载态与空态全局统一。
- 用户可见区域不展示数据库 ID；关联名称由后端直出，前端不做跨表拼接。
- 日期 `YYYY-MM-DD`、时间 `YYYY-MM-DD HH:mm:ss`、金额两位小数；考勤单位展示为天（8 小时 = 1 天，工具函数统一换算）。
- 审计详情自动过滤技术字段与敏感路径。

### 4.6 首次登录

登录返回首次登录标记时弹出不可关闭的强制改密弹窗，新密码不得与默认密码相同，完成后进入系统。

## 五、数据模型设计

### 5.1 事件源层

- **position_events**：职务变更事实，仅存储变更字段，未变更字段为 NULL；入职/离职/调薪调岗语义由事件类型定义。
- **attendance_daily + attendance_event_details**：天维度主表 + 事件详情子表；主表携带确认状态（pending/confirmed），详情区分出勤/休假/加班/违纪/打卡时间戳五类。
- **annual_leave_account_events**：年假额度变动流水（配发/结转扣除/人工调整），系统周期事件仅可经批次生成与冲销。
- **salary_events**：工资调整事件（绩效系数/提成/奖惩/借款还款/个税扣除），归属月份手动指定。

### 5.2 基础投影层

- **position_snapshots**：职务时段快照，时段连续不重叠，最新段以 9999-12-31 收尾。
- **attendance_daily_projections**：日考勤记工时，含记出勤/工作日加班/节假日加班/违纪/事假标记，pending 状态不参与核算。
- **annual_leave_balance_snapshots / leave_in_lieu_balance_snapshots**：年假与调休余额时段快照，余额允许负值。

### 5.3 业务核算层

- **attendance_calculation_monthly**：月度考勤核算，聚合日记工时与加权职务快照，产出出勤工资、加班工资、全勤奖。

### 5.4 结果汇总层

- **salary_summaries**：单人员单月最新核算结果。
- **salary_summary_versions**：全量版本留存，含操作人与批次号，支持任意两版本对比。

### 5.5 注册与关联层

- persons 及电话/邮箱/银行卡子表、companies、files、file_relations，均全局软删除，人员/公司删除级联子表与事件源，恢复同步级联。

### 5.6 支撑层

- audit_logs（仅追加）、sys_config（键值 + 元数据）、users/roles/permissions 及关联表、sys_batches（批次）、schema_migrations（版本化迁移）。

## 六、核心业务规则

### 6.1 职务与人员

- 身份证号未删除记录全局唯一（部分唯一索引，空串不参与）。
- 离职以职务事件表达，不删除基础数据；删除人员级联软删全部原始数据，投影重建自动过滤。
- 职务事件增删改自动全量重建时段快照；同日多事件按 seq 叠加，序号大者覆盖同名字段。

### 6.2 考勤

- 事件增删改按子类型精细触发投影：年假触发年假余额、补班/调休触发调休余额、其余仅重建日记工时；变更前后并集判定，删除与类型变更同样生效。
- 余额计算仅计入已确认记录，过滤软删除数据。
- 钉钉月度汇总导入：上传 → 姓名匹配预览（精确/模糊/未匹配）→ 确认导入；请假不满 8 小时、迟到超 30 分钟、缺卡、旷工、休息并打卡自动标记待确认。

### 6.3 假期权益

- 年假余额 = 配发 + 人工调整 − 结转扣除 − 已休（确认记录）。
- 调休余额 = 累计补班 − 累计调休，永久有效。
- 周年结转：按入职月为周年周期，月末扣减、月初配发；重复执行幂等（先冲销历史同周期批次事件，批次记录不保留）；反结账整体事务化，事件变动由审计留痕。

### 6.4 薪资核算

- 出勤工资 = 记出勤工时 × 加权基本工资 / 计薪天数 / 日工时基准。
- 加班工资按工作日 1.5 倍、节假日 2.0 倍系数（可配置）。
- 全勤奖 =（记出勤天数 − 违纪次数）× 日标准；当月存在无全勤奖时段或有事假则清零。
- 绩效与补贴：全月在职直取定额；入职/离职月按实际出勤比例折算，代扣项永不折算。
- 年假结转工资 = 结转时长 ×（加权基本工资 + 加权餐补）/ 计薪天数 / 日工时基准 × 节假日系数。
- 每次核算生成独立版本；下层数据变动（考勤核算/职务快照/工资事件/年假权益事件）自动标记「数据已变动」，导出前二次确认。
- 全链路追溯：汇总 → 考勤核算 → 每日明细 → 原始事件/职务快照/工资事件/结转事件。

### 6.5 审计

- action 枚举：新增/修改/删除/恢复/核算/确认/结转/反结账/配置修改。
- 修改与删除的 before/after 均为完整数据快照；恢复由 deleted_at 置空自动识别。
- 文件上传/删除/恢复/关联全部纳入审计，详情过滤服务器路径等敏感字段。

### 6.6 安全

- JWT 登出即时失效（内存黑名单 + 惰性清理）；token 含随机标识保证唯一。
- 上传限制：大小上限可配置（默认 50MB），可执行扩展名黑名单拦截。
- 密码 bcrypt 存储，首次登录强制改密。

## 七、工程化与部署

- 版本化数据库迁移（schema_migrations），表结构与索引由迁移管理，禁止修改已发布条目。
- 测试体系：后端核算/审计/结转/导入/投影边界单元测试，前端工具函数单测；构建脚本以 vet、test、类型检查、lint 为硬门槛。
- 服务支持 SIGINT/SIGTERM 优雅退出；CORS 白名单可配置。
- 单二进制交付：构建脚本一键产出前端嵌入产物，部署目录即二进制同级目录，配置与数据自动生成。

## 八、未来演进方向

### 8.1 考勤智能化

- 排班管理：班次模板、轮班规则，自动生成应出勤基准。
- 打卡校准：迟到/早退自动判定、异常打卡提示。
- 缺勤预警：按规则自动生成缺卡/旷工候选，减少人工核对。

### 8.2 审批流引擎

- 请假/加班/调休/离职线上审批，审批通过自动生成事件源，形成「申请 → 审批 → 入账」闭环。
- 审批链路可配置（按职务/部门/金额），与审计体系贯通。

### 8.3 工资条

- 在线工资条发放与签收确认，明细按项目加密展示，签收留痕。

### 8.4 多主体支持

- 多公司/多账套数据隔离，集团级汇总报表。
- 公司间调动与借调场景。

### 8.5 报表中心

- 自定义报表与图表（考勤汇总、人力成本、离职率等），同比环比与导出。
- 报表基于投影层只读查询，不引入新写路径。

### 8.6 消息通知

- 站内信、邮件、钉钉/企业微信推送模板化，覆盖待确认提醒、核算完成、审批通知。

### 8.7 定时任务

- 周年结转、月度核算提醒、待确认超时提醒由调度器自动执行，与手动操作幂等共存。

### 8.8 开放能力

- 面向财务/HRIS 的只读 API 与 Webhook；OAuth2 客户端管理。
- 钉钉/企业微信组织架构同步与免登。

### 8.9 数据能力

- 全量备份/恢复与导出归档；导入模板化（人员/职务/工资事件）。
- 数据字典与字段级文档化。

### 8.10 审计增强

- 操作行为分析（高频操作、异常时段）；合规导出报告。
- 审计日志哈希链校验，防篡改可验证。

### 8.11 移动端

- H5/小程序：考勤打卡、审批、余额与工资条查询。

### 8.12 运维可观测

- 请求日志与慢查询统计、指标采集（Prometheus 格式）、健康检查扩展。

### 8.13 前端工程演进

- 组件文档站（Storybook 模式）。
- 表单 Schema 驱动化：从字段配置演进到完整 Schema 渲染，动态表单与联动校验。
- 暗色主题与主题定制。
- 组件层测试扩展（在工具函数单测基础上覆盖核心通用组件）。

### 8.14 后端工程演进

- 数据库适配层抽象，支持 SQLite 与 PostgreSQL 切换。
- Docker 镜像与 CI/CD 流水线，一键发布。
- 多语言与多时区支持。
