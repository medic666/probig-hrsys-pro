# 企业人事与行政管理系统企划

## 第一部分 系统企划书
### 一、项目概述
#### 1.1 项目定位
本系统是面向中小企业的轻量生产级人事与行政管理系统，以**事件驱动+多层投影**为核心架构，支持人员、公司、考勤、薪资、文件、审计全流程管理。

#### 1.2 核心设计理念
- **事件驱动**：所有业务事实以原始事件形式存储；任何结果数据均可由原始事件全量重建，保证数据可追溯、可复现。
- **多层投影聚合**：采用「事件源→基础投影→业务核算→结果汇总」的四级分层模型，上层投影仅依赖下层数据生成，不允许直接修改投影层。
- **审计全覆盖**：事件源操作全程留痕，审计日志仅追加、不可删除，保证数据合规可追溯，满足人事管理的审计要求。
- **容错性设计**：事件源软删除机制，误删数据可一键恢复，操作风险极低。
- **开箱即用**：服务启动自动初始化默认配置、默认管理员账号与基础权限体系，零配置即可运行。
- **权限可控**：完整RBAC权限体系，权限控制到模块级读、写、删、导出，前端后端双重校验，数据安全可控。

#### 1.3 多层事件驱动架构总览

| 层级 | 统一命名 | 核心内容 | 触发方式 | 核心特性 |
|------|----------|----------|----------|----------|
| 第0层 | 事件源层 | 职务事件、考勤事件、年假权益事件、工资调整事件 | 用户手动录入/导入、系统周期操作生成 | 原始事实，操作留痕，顺序唯一 |
| 第1层 | 基础投影层 | 职务时段快照、日考勤记工时、年假余额、调休余额 | 事件变动后自动重算 | 粒度最细，实时一致；可全量重建 |
| 第2层 | 业务核算层 | 月度考勤核算| 用户手动触发核算 | 专项业务结果；基于基础投影计算 |
| 第3层 | 结果汇总层 | 月度工资总表 | 用户手动触发核算 | 跨模块聚合最终结果；全链路可追溯，支持多版本留存 |

> 下层数据改动后，通过「下层数据最后更新时间 > 本层最后核算时间」判定数据是否过期，过期统一标记为「数据已变动」，提醒用户重算上层投影，兼顾业务补录的灵活性与核算结果的准确性。
---

### 二、技术栈选型
#### 2.1 后端技术栈
- 核心框架：Go + Gin
- ORM：GORM
- 数据库：SQLite（无需额外部署数据库服务，原生支持部分索引、CHECK约束）
- 认证：golang-jwt + bcrypt 密码加密
- 静态资源嵌入：Go embed（前端打包后嵌入二进制，实现单文件部署）
- 文件存储：本地文件系统（数据库存储元数据与文件路径）
- 依赖原则：极简依赖，仅引入必要组件，避免过重框架，降低维护成本

#### 2.2 前端技术栈
- 构建工具：Bun + Vite
- 核心框架：Vue3 + TypeScript
- UI组件库：Element Plus + @element-plus/icons-vue
- 路由模式：Hash模式（适配单文件部署，无需后端fallback配置）
- 状态管理：Pinia
- 请求库：Axios
- 日期工具：dayjs

---

### 三、数据模型设计

#### 3.1 事件源层：原始业务事件
所有原始事实数据，改动留痕，全局软删除；所有枚举类型字段均设置数据库CHECK约束，限定合法取值范围，作为应用层校验的兜底保障；单人员内事件分配单调递增序列号，保证事件顺序绝对唯一、重建结果幂等。

##### 3.1.1 职务事件表（position_events）
记录人员所有职务变动，仅存储发生变更的字段，未变更字段为NULL；事件仅携带生效日期，入职、离职等业务语义由事件类型定义。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| seq | int | 单人员内事件序列号，单调递增 |
| event_name | varchar(128) | 事件名称（如入职、调薪调岗、离职） |
| effective_date | date | 事件生效日期 |
| entry_date | date | 入职日期 |
| leave_date | date | 离职日期（在职为NULL） |
| attendance_group | varchar(64) | 考勤组 |
| has_annual_leave | bool | 享有年假标识（外包人员关闭） |
| has_attendance_bonus | bool | 享有全勤奖标识（外包人员关闭） |
| base_salary | decimal(10,2) | 基本工资 |
| performance_salary | decimal(10,2) | 绩效工资基数 |
| salary_days | int | 计薪天数 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴月标准 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

> 核心规则：
> 1. 单人员内建立`(person_id, seq)`联合唯一索引，保证序号不重复、不跳号；同日多条事件按seq升序叠加，序号大的覆盖序号小的同名字段
> 2. 增删改任意事件后，自动触发该员工职务时段快照全量重建

##### 3.1.2 考勤事件表（attendance_events）
单表存储全类型考勤明细事件，所有出勤、休假、加班、违纪均为客观打卡事实。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| seq | int | 单人员内事件序列号，单调递增 |
| event_date | date | 事件日期（自然日） |
| punch_time | varchar(32) | 打卡时间（备注字段，不参与计算，仅留痕） |
| event_type | varchar(32) | 事件类型：出勤/休假/加班/违纪 |
| sub_type | varchar(32) | 子类型：<br>出勤：普通出勤、补班出勤、外勤出勤<br>休假：调休、事假、病假、年假、法定假、福利假<br>加班：工作日加班、节假日加班<br>违纪：缺卡、迟到、早退 |
| hours | decimal(4,1) | 事件时长（小时精度） |
| remark | varchar(256) | 备注 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

> 核心规则：
> 1. 单人员内建立`(person_id, seq)`联合唯一索引；小时精度，单日允许多条同类型事件，仅重复提示不强制拦截，适配补录调账场景
> 2. 前端支持跨天录入、多人员批量录入，后端自动拆分为单日单条事件
> 3. 增删改任意事件后，自动触发对应人员对应日期的「日考勤记工时」重算；年假类考勤事件同步触发年假余额重算；补班、调休类事件同步触发调休余额重算

##### 3.1.3 年假权益事件表（annual_leave_account_events）
仅记录年假权益的额度变动流水，是额度余额的权益类真相源，不承载休假事实记录。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| seq | int | 单人员内事件序列号，单调递增 |
| event_type | varchar(32) | 事件类型：grant（配发）、carryover_deduct（结转扣除）、adjust（人工调整） |
| source_type | varchar(16) | 事件来源：manual（人工录入）、system_period（系统周期事件） |
| batch_id | uint | 关联批次ID，系统周期事件必填，人工事件为空 |
| hours | decimal(5,1) | 额度变动值：配发和结转扣除都为正数，配发增加额度，扣除减少额度。人工调整可正可负 |
| effective_date | date | 生效日期 |
| remark | varchar(256) | 备注 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

> 核心规则：
> 1. 单人员内建立`(person_id, seq)`联合唯一索引；系统周期事件仅可通过批次操作生成与冲销，不提供单条编辑接口
> 2. 增删改任意事件后，自动触发对应人员对应类型的年假余额重算

##### 3.1.4 工资调整事件表（salary_events）
工资调整事件，支持手动指定归属月份，全局软删除。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| seq | int | 单人员内事件序列号，单调递增 |
| belong_month | varchar(7) | 归属月份（格式：YYYY-MM，手动指定） |
| event_type | varchar(32) | 事件类型：绩效系数、提成、奖惩、借款还款、个税扣除 |
| amount | decimal(10,2) | 金额/系数值：<br>绩效系数:正数系数<br>个税扣除：正数扣除额<br>其他：可正可负金额 |
| remark | varchar(256) | 备注 |
| created_at/updated_at/deleted_at | datetime | 软删除标记 |

> 核心规则：
> 1. 同月多条绩效系数事件：取seq最大的系数值（覆盖式）
> 2. 同月其它工资事件：所有金额按规则累加计算
> 3. 金额约定：个税扣除类填正数，核算时自动从应发工资扣减；提成、奖惩、借款还款等可正可负，正数为增发、负数为扣减

#### 3.2 基础投影层（第1层投影）
由原始事件自动投影生成，粒度最细，实时更新，是上层核算的基础。

##### 3.2.1 职务时段快照（position_snapshots）
由职务事件全量重建生成，以时段为单位存储完整职务信息。同一在职周期内的所有快照时段连续、不重叠、无空窗。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| effective_start_date | date | 快照生效起始日期（含） |
| effective_end_date | date | 快照生效结束日期（含）；最新有效记录存`9999-12-31`表示长期有效 |
| is_active | bool | 在职标识 |
| entry_date | date | 入职日期 |
| leave_date | date | 离职日期（在职为NULL） |
| attendance_group | varchar(64) | 考勤组 |
| has_annual_leave | bool | 享有年假标识 |
| has_attendance_bonus | bool | 享有全勤奖标识 |
| base_salary | decimal(10,2) | 基本工资 |
| performance_salary | decimal(10,2) | 绩效工资基数 |
| salary_days | int | 计薪天数 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴标准 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| last_calc_at | datetime | 最后重建时间 |
| created_at/updated_at | datetime | 时间戳 |

> 重建规则：
> 1. 触发时机：职务事件增删改、恢复后自动触发全量重建
> 2. 事件排序：按`effective_date`升序遍历事件链，同日事件按seq升序叠加字段值
> 3. 时段生成：每遇到字段变更即生成新的快照时段，前一时段结束日期 = 新事件生效日期 - 1天，最新一段快照结束日期设为`9999-12-31`

##### 3.2.2 日考勤记工时（attendance_daily_projection）
由当日所有考勤事件自动聚合生成，每日一条，是薪资核算的标准化考勤输入。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| work_date | date | 统计日期 |
| punch_time | varchar(32) | 打卡时间（来自考勤事件表） |
| work_hours | decimal(4,1) | 记出勤工时（小时） |
| overtime_workday_hours | decimal(4,1) | 记工作日加班工时（小时） |
| overtime_holiday_hours | decimal(4,1) | 记节假日加班工时（小时） |
| has_personal_leave | bool | 当日是否有事假（用于全勤奖判定） |
| violation_count | int | 当日违纪次数 |
| remark | varchar(256) | 备注（来自考勤事件表） |
| last_calc_at | datetime | 最后重算时间 |
| created_at/updated_at | datetime | 时间戳 |

> 投影规则：
> ```
> 记出勤工时 = 普通出勤时长 + 补班出勤时长 + 外勤出勤时长
>            + 病假时长 × 病假系数
>            + 年假时长 + 调休时长 + 法定假时长 + 福利假时长
> ```
> - 工作日加班工时：累加当日所有工作日加班事件时长
> - 节假日加班工时：累加当日所有节假日加班事件时长
> - 事假不计入记出勤工时，仅标记`has_personal_leave`
> - 违纪事件每条计1次，当日多条违纪累计次数
> 触发机制：
> - 考勤事件增删改后，自动重算对应人员对应日期的记工时数据

##### 3.2.3 年假余额时段快照（annual_leave_account_balance_snapshots）
由年假权益事件与考勤事件聚合生成，实时计算年假可用额度。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| effective_start_date | date | 快照生效起始日期（含） |
| effective_end_date | date | 快照生效结束日期（含）；最新有效记录存`9999-12-31`表示长期有效 |
| balance_hours | decimal(5,1) | 年假剩余额度（小时，允许负值） |
| last_calc_at | datetime | 最后重算时间 |
| created_at/updated_at | datetime | 时间戳 |

> 投影规则：
> - 年假余额 =（配发+人工调整-结转扣除）累计值 - 考勤事件中年假休假累计时长
> 触发机制：
> - 年假权益事件增删改后，自动触发对应人员对应类型的余额重算
> - 考勤事件中年假类事件增删改后，自动触发对应人员对应类型的余额重算
> - 时段生成：每遇到字段变更即生成新的快照时段，前一时段结束日期 = 新事件生效日期 - 1天，最新一段快照结束日期设为`9999-12-31`

##### 3.2.4 调休余额时段快照（leave_in_lieu_balance_snapshots）
由考勤事件中补班、调休类事件聚合生成，实时计算调休可用额度。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| effective_start_date | date | 快照生效起始日期（含） |
| effective_end_date | date | 快照生效结束日期（含）；最新有效记录存`9999-12-31`表示长期有效 |
| balance_hours | decimal(5,1) | 调休剩余额度（小时，允许负值） |
| last_calc_at | datetime | 最后重算时间 |
| created_at/updated_at | datetime | 时间戳 |

> 投影规则：
> - 调休余额 = 累计补班出勤时长 - 累计调休时长
> 触发机制：
> - 考勤事件中补班、调休类事件增删改后，自动触发对应人员的调休余额全量重算
> - 时段生成：每遇到余额变动即生成新的快照时段，前一时段结束日期 = 新事件生效日期 - 1天，最新一段快照结束日期设为`9999-12-31`

#### 3.3 业务核算层（第2层投影）
基于基础投影计算的专项业务结果，手动触发核算。

##### 月度考勤核算表（attendance_calculation_monthly）
按月度聚合日考勤记工时，与当月在职日的加权职务快照结合，计算出勤工资、加班工资、全勤奖，是薪资核算的专项中间结果。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份 |
| salary_days | int | 加权计薪天数 |
| weighted_base_salary | decimal(10,2) | 加权基本工资 |
| weighted_meal_allowance | decimal(10,2) | 加权餐补 |
| total_work_hours | decimal(5,1) | 当月累计记出勤工时 |
| total_overtime_workday_hours | decimal(5,1) | 当月累计工作日加班工时 |
| total_overtime_holiday_hours | decimal(5,1) | 当月累计节假日加班工时 |
| attendance_salary | decimal(10,2) | 月度出勤工资 |
| overtime_workday_salary | decimal(10,2) | 月度工作日加班工资 |
| overtime_holiday_salary | decimal(10,2) | 月度节假日加班工资 |
| has_personal_leave_month | bool | 当月是否有事假记录 |
| total_violation_count | int | 当月违纪总次数 |
| attendance_bonus | decimal(10,2) | 月度全勤奖 |
| last_calc_at | datetime | 最后核算时间 |
| created_at/updated_at | datetime | 时间戳 |

> 核算规则：
> 1. **出勤工资**：`当月累计记出勤工时 × (加权基本工资 / 计薪天数 / 8)`
> 2. **工作日加班工资**：`当月累计工作日加班工时 × (加权基本工资 + 加权餐补) / 计薪天数 / 8 × 工作日加班系数`
> 3. **节假日加班工资**：`当月累计节假日加班工时 × (加权基本工资 + 加权餐补) / 计薪天数 / 8 × 节假日加班系数`
> 4. **全勤奖**：
>    - 前置校验1：当月职务快照`has_attendance_bonus`存在为false的时段 → 全勤奖=0
>    - 前置校验2：当月有事假记录 → 全勤奖=0
>    - 正常计算：`全勤奖 = (当月记出勤总工时 / 8 - 当月违纪总次数) × 全勤奖日标准`，最低为0
>
> 触发机制：
> - 用户手动触发核算，支持按月份、人员范围批量核算
> - 日考勤记工时或职务快照变动后，标记对应月份考勤工资为「数据已变动」

#### 3.4 结果汇总层（第3层投影）
跨模块聚合的最终业务结果，手动触发核算，支持多版本历史留存。

##### 3.4.1 月度工资总表（salary_summaries）
存储单人员单月份最新核算结果，为日常查询、统计的主表。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份 |
| salary_days | int | 计薪天数（取自月度考勤核算） |
| weighted_base_salary | decimal(10,2) | 加权基本工资（取自月度考勤核算） |
| total_work_hours | decimal(5,1) | 累计记出勤工时 （取自月度考勤核算）|
| total_overtime_workday_hours | decimal(5,1) | 累计工作日加班工时（取自月度考勤核算） |
| total_overtime_holiday_hours | decimal(5,1) | 累计节假日加班工时 （取自月度考勤核算）|
| attendance_salary | decimal(10,2) | 出勤工资（取自月度考勤核算） |
| overtime_workday_salary | decimal(10,2) | 工作日加班工资 （取自月度考勤核算）|
| overtime_holiday_salary | decimal(10,2) | 节假日加班工资 （取自月度考勤核算）|
| annual_leave_carryover_deduct | decimal(5,1) | 年假结转时长（取自年假权益事件） |
| annual_leave_carryover_salary | decimal(10,2) | 年假结转工资 |
| attendance_bonus | decimal(10,2) | 全勤奖（取自月度考勤核算） |
| performance_salary | decimal(10,2) | 最终绩效工资 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| sales_commission | decimal(10,2) | 提成 |
| reward_punishment | decimal(10,2) | 奖惩 |
| borrowing_repayment | decimal(10,2) | 借款还款 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| tax_deduct | decimal(10,2) | 个税代扣 |
| final_salary | decimal(10,2) | 实发工资 |
| last_calc_at | datetime | 最后核算时间 |
| created_at/updated_at | datetime | 时间戳 |

##### 3.4.2 月度工资核算历史版本表（salary_summary_versions）
全量留存每次核算的完整结果，支持历史追溯与版本对比。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| person_id | uint | 关联人员ID |
| belong_month | varchar(7) | 归属月份 |
| version | int | 版本号，单人员单月份内从1开始递增 |
| calc_batch_no | varchar(64) | 核算批次号，同一次批量核算共享同一批次号 |
| operator_id | uint | 核算操作人ID |
| operator_name | varchar(64) | 核算操作人姓名（冗余存储） |
| salary_days | int | 计薪天数 |
| weighted_base_salary | decimal(10,2) | 加权基本工资 |
| total_work_hours | decimal(5,1) | 累计记出勤工时 |
| total_overtime_workday_hours | decimal(5,1) | 累计工作日加班工时 |
| total_overtime_holiday_hours | decimal(5,1) | 累计节假日加班工时 |
| attendance_salary | decimal(10,2) | 出勤工资 |
| overtime_workday_salary | decimal(10,2) | 工作日加班工资 |
| overtime_holiday_salary | decimal(10,2) | 节假日加班工资 |
| annual_leave_carryover_deduct | decimal(5,1) | 年假结转时长 |
| annual_leave_carryover_salary | decimal(10,2) | 年假结转工资 |
| attendance_bonus | decimal(10,2) | 全勤奖 |
| performance_salary | decimal(10,2) | 最终绩效工资 |
| post_allowance | decimal(10,2) | 职位津贴 |
| meal_allowance | decimal(10,2) | 餐补 |
| housing_allowance | decimal(10,2) | 房补 |
| transport_allowance | decimal(10,2) | 交通补贴 |
| high_temp_allowance | decimal(10,2) | 高温补贴 |
| insurance_compensation | decimal(10,2) | 保险补偿 |
| fund_compensation | decimal(10,2) | 公积金补偿 |
| sales_commission | decimal(10,2) | 提成 |
| reward_punishment | decimal(10,2) | 奖惩 |
| borrowing_repayment | decimal(10,2) | 借款还款 |
| social_security_deduct | decimal(10,2) | 社保代扣 |
| housing_fund_deduct | decimal(10,2) | 公积金代扣 |
| tax_deduct | decimal(10,2) | 个税代扣 |
| final_salary | decimal(10,2) | 实发工资 |
| created_at | datetime | 版本生成时间 |

> 核算规则：
> 1. **考勤基础工资项**：出勤工资、工作日加班工资、节假日加班工资、全勤奖，直接读取当月考勤工资表数据
> 2. **年假结转工资**：`当月年假权益事件carryover_deduct时长 × (加权基本工资 + 加权餐补) / 计薪天数 / 8 × 节假日加班系数`
> 3. **绩效工资**：
>    - 月度绩效基数：按当月在职日职务快照绩效基数加权平均
>    - 绩效系数：同月多条绩效事件取seq最大的值，无则默认1
>    - 默认全额发放，入职/离职月按当月在职天数比例折算
> 4. **补贴项**：
>    - 高温补贴：当月属于配置发放月份则全额发放，否则为0。
>    - 补贴项默认全额发放，入职/离职月按在职天数比例折算
> 5. **调整项合计**：所有提成、奖惩、借款还款事件金额累加，可正可负，入职/离职月不折算
> 6. **代扣项**：社保代扣 + 公积金代扣 + 个税代扣；其中社保、公积金取当月职务快照加权值，个税取当月工资事件个税扣除累计值，代扣项入职/离职月不折算
> 7. **实发工资** = 出勤工资 + 工作日加班工资 + 节假日加班工资 + 未休年假折算工资 + 全勤奖 + 绩效工资 + 各项补贴 + 调整项合计 - 各项代扣
> 8. 每次核算生成新版本记录并写入历史版本表，同步更新工资总表最新数据
>
> 数据变动检测：
> 满足以下任一条件，标记为「数据已变动」：
> 1. 对应月度考勤核算表的`last_calc_at` > 工资汇总`last_calc_at`
> 2. 生效日期在当月的职务快照表的`last_calc_at`  > 工资汇总`last_calc_at`
> 3. 当月工资事件的`max(updated_at, deleted_at)` > 工资汇总`last_calc_at`
> 4. 当月年假权益事件的`max(updated_at, deleted_at)` > 工资汇总`last_calc_at`

#### 3.5 注册层与关联层
##### 3.5.1 人员主表（persons）
存储人员基础身份信息，全局软删除；所有在职属性均从职务快照实时读取。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uint | 主键 | 系统内部ID，前端不展示 |
| name | varchar(64) | 非空 | 人员姓名 |
| id_card | varchar(32) | 部分唯一索引 | 身份证号，未删除记录全局唯一 |
| gender | tinyint | | 性别 |
| birthday | date | | 生日 |
| nation | varchar(32) | | 民族 |
| native_place | varchar(128) | | 籍贯 |
| address | varchar(256) | | 住址 |
| political_status | varchar(32) | | 政治面貌 |
| marital_status | tinyint | | 婚姻状态 |
| alias | varchar(64) | | 别名 |
| created_at/updated_at/deleted_at | datetime | | 软删除标记 |

> 唯一约束规则：采用部分唯一索引，仅对`deleted_at IS NULL`的记录生效，保证未删除人员身份证号全局唯一；已删除记录不参与唯一校验，证件号码可被新主体复用；恢复已删除人员时前置校验证件号码是否被占用。

##### 3.5.2 人员关联子表（一对多，级联软删除）
- 人员电话表（person_phones）：支持多条
- 人员邮箱表（person_emails）：支持多条
- 人员银行卡表（person_bank_cards）：支持多条

##### 3.5.3 公司主表（companies）
存储公司基础信息，全局软删除。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | uint | 主键 | 系统内部ID |
| name | varchar(128) | 非空 | 公司名称 |
| credit_code | varchar(64) | 部分唯一索引 | 统一社会信用代码，未删除记录全局唯一 |
| address | varchar(256) | | 地址 |
| contact_phone | varchar(32) | | 联系电话 |
| bank_name | varchar(64) | | 开户行 |
| bank_account | varchar(64) | | 银行账号 |
| created_at/updated_at/deleted_at | datetime | | 软删除标记 |

> 唯一约束规则：采用部分唯一索引，仅对`deleted_at IS NULL`的记录生效，保证未删除公司统一社会信用代码全局唯一；已删除记录不参与唯一校验，代码可被新主体复用；恢复已删除公司时前置校验信用代码是否被占用。

##### 3.5.4 文件主表与关联表
- 文件主表（files）：统一存储文件元数据，全局软删除，文件本体存放本地文件系统
- 文件关联中间表（file_relations）：实现文件与业务对象的多对多关联，全局软删除

#### 3.6 审计层
##### 审计日志表（audit_logs）
仅追加、不可删除，无软删除字段，所有写操作永久留存。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| operator_id | uint | 操作人ID |
| operator_name | varchar(64) | 操作人姓名（冗余存储） |
| target_type | varchar(64) | 操作对象类型 |
| target_id | uint | 操作对象ID |
| target_name | varchar(128) | 操作对象名称（冗余存储，列表直接展示） |
| action | varchar(32) | 操作类型：新增/修改/删除/恢复/核算/配置修改/结转/反结账等 |
| before_snapshot | text | 操作前完整数据快照（JSON） |
| after_snapshot | text | 操作后完整数据快照（JSON） |
| ip | varchar(64) | 操作IP |
| created_at | datetime | 操作时间 |

> 统一规则：
> 1. 快照填充：新增操作`before_snapshot`为空；删除操作`after_snapshot`为空；修改/恢复操作前后快照均填充完整数据
> 2. 批量操作：所有批量操作均拆解为单条业务实体维度的审计记录，每条独立存储对应实体的前后快照
> 3. 操作约束：仅支持查询和导出，无任何修改/删除接口，永久留存

#### 3.7 系统配置层
##### 系统配置表（sys_config）
键值对存储全局配置，服务启动时全量加载到内存缓存，修改后实时生效。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 主键 |
| config_key | varchar(64) | 配置键（模块.配置项格式，唯一索引） |
| config_value | text | 配置值（字符串存储） |
| config_name | varchar(128) | 配置显示名称 |
| config_desc | varchar(256) | 配置说明 |
| value_type | varchar(16) | 值类型：string/number/bool/select |
| option_values | text | 下拉可选值（JSON数组） |
| updated_at | datetime | 更新时间 |

> 核心可配置项清单：
> - 系统类：分页默认条数、导出最大条数、文件上传大小限制、文件存储根路径、计薪小时基准（默认8小时/天）
> - 考勤类：最小请假单位、病假系数、工作日加班系数、节假日加班系数、全勤奖日标准、高温补贴发放月份
> - 年假类：年假年度额度、年假周期规则（入职周年或自然周年）

#### 3.8 权限层（RBAC0模型）
- 用户表（users）：系统登录账号，关联人员表
- 角色表（roles）：角色定义与备注
- 权限表（permissions）：模块级权限，粒度为「模块+操作」
- 用户角色关联表、角色权限关联表：多对多关联

#### 3.9 通用批次层
##### 通用批次表（sys_batches）
全系统周期类操作的统一批次管理载体，支持所有业务场景的结账、反结账能力。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | uint | 批次ID |
| batch_no | varchar(64) | 全局唯一批次号 |
| business_type | varchar(32) | 业务类型：annual_leave_carryover等 |
| business_period | varchar(32) | 业务周期：如2025-06、第3周年 |
| operator_id | uint | 操作人ID |
| operator_name | varchar(64) | 操作人姓名（冗余） |
| status | tinyint | 状态：1-待执行 2-已生效 3-已冲销 4-执行失败 |
| total_count | int | 处理数量 |
| remark | varchar(256) | 备注 |
| created_at | datetime | 创建时间 |
| executed_at | datetime | 执行完成时间 |
| canceled_at | datetime | 冲销时间 |

---

### 四、核心业务逻辑
#### 4.1 人员管理
- 展示人员列表(枚举人员主表)，展示部分信息如姓名、职务、在职状态等
- 详细展示人员的基础信息(包含多值字段如电话、邮箱、银行卡)，展示人员的职务信息(取自当前日职务快照)，展示人员的职务信息变动历史(取自所有历史职务事件)，展示人员的关联附属文件(取自文件关联表)
- 人员附件关联文件管理
- 改动人员的基础信息(更改人员主表)
- 新增人员(建立人员主表)
- 为人员添加职务事件，如入职事件，调薪调岗事件，离职事件等。查看并修改人员的职务事件。(增删改该人员的职务事件)
- 身份证号未删除记录全局唯一校验，重复录入拦截
- 离职不删除人员基础信息主表、子表、文件关联、事件源等原始数据，仅在职务事件中添加离职事件即可。
- 删除人员(慎用)仅级联软删除事件源、主表、子表、文件关联等原始数据，不级联删除投影数据；投影为衍生数据，重建时自动过滤已软删除的事件源
- 恢复人员同步恢复所有原始数据，恢复前校验身份证号未被占用，恢复后可手动全量重建投影
- 有搜索、筛选、分页、Excel导出等功能

#### 4.2 公司管理
- 展示公司列表(枚举公司主表)
- 详细展示公司的基础信息，展示公司的关联附属文件(取自文件关联表)
- 改动公司的基础信息(更改公司主表)
- 新增公司(建立公司主表)
- 统一社会信用代码未删除记录全局唯一校验
- 公司附件关联文件管理
- 删除公司(慎用)级联软删除主表、子表、文件关联等原始数据
- 恢复公司同步恢复所有原始数据，恢复前校验统一社会信用代码未被占用
- 有搜索、筛选、分页、Excel导出等功能

#### 4.3 考勤管理
##### 4.3.1 考勤事件管理
- 支持按人员、时间、类型多维度筛选查询，分页、导出
- 支持单条创建、跨天创建、批量创建，后端自动拆分单日事件并按人员分配连续序列号
- 同日同类型事件重复仅提示不强制拦截，适配补录调账场景
- 事件增删改后，自动触发对应日期的日考勤记工时重算；年假类事件同步触发年假余额重算；补班、调休类事件同步触发调休余额重算
- 考勤事件录入、存储与计算统一使用小时为单位

##### 4.3.2 日考勤记工时查询
- 支持按人员、日期范围查询每日记工时明细
- 展示每日打卡时间、记出勤工时、工作日加班工时、节假日加班工时、违纪次数、是否有事假、备注
- 支持追溯查看当日所有原始考勤事件
- 数据为自动投影生成，仅支持查询，不支持手动修改

##### 4.3.3 月度考勤核算
- 支持按月份、人员范围筛选核算
- 基于日考勤记工时 + 职务时段快照加权计算
- 自动跳过当月无在职记录的人员
- 下层数据变动后自动标记「数据已变动」状态
- 导出时若存在变动数据，弹出二次确认

##### 4.3.4 钉钉导入预留框架
- 完整流程框架：文件上传 → 解析占位 → 结果预览 → 确认入库 → 错误提示
- 导入事件与手动录入事件规则完全一致，自动拆分、自动分配序列号、自动重算投影、自动审计

#### 4.4 假期权益管理
##### 4.4.1 年假
###### 4.4.1.1 年假事件管理
- 支持按人员、时间范围查询年假权益事件+假勤事件中的休年假事件。
- 支持人工录入年假权益事件，人工录入事件可编辑、删除。
- 系统周年事件仅可查看，不可单条编辑、删除
- 所有额度变动均计入审计日志
###### 4.4.1.2 余额查询
- 支持按人员查询实时可用额度，数据取自年假余额时段快照
- 支持下钻查看额度明细构成：累计配发、累计已休、累计结转、累计人工调整
- 数据为自动投影生成，仅支持查询，不支持手动修改
###### 4.4.1.3 年假周年事件
- **周期规则**：默认以入职所在月为周年周期起始，每周年初配发一次年度标准额度，每周年末结转一次剩余年假。
- **资格管控**：职务快照中「享有年假」标识为关闭的员工，不参与结转
- **结转规则**：
  1.  周期末余额>0时，生成结转扣减事件，扣减年假额度；
  2.  周期末余额≤0时，不生成结转事件，负余额自动滚存至下一周期
  3.  周期初生成标准配发事件，额度为系统配置的年度标准值，与当前余额无关；
- **批量结转操作**：
  1.  选择目标月份，系统自动筛选所有「周年日处于次月」的在职且享有年假的员工
  2.  逐员工执行：冲销历史同周期批次 → 计算周期末余额 → 生成新批次结转与配发事件
  3.  全程包裹事务，失败自动回滚，支持幂等重复触发
- **反结账操作**：支持对指定批次执行反结账，软删除该批次所有系统事件，余额自动回到结账前状态

##### 4.4.2 调休
###### 4.4.2.1 调休事件流水
- 支持按人员、时间范围查询考勤事件中的补班、调休事件
- 支持人工录入补班、调休考勤事件，修改已有考勤事件
###### 4.4.2.2 余额查询
- 支持按人员查询实时可用额度，数据取自调休余额时段快照，实时余额=累计补班-累计调休
- 支持下钻查看额度构成明细：累计补班、累计调休
- 数据为自动投影生成，仅支持查询，不支持手动修改
- 余额永久有效，支持负余额延续

#### 4.5 工资管理
##### 4.5.1 工资事件管理
- 支持按人员、月份、类型筛选查询，分页、导出
- 事件支持手动指定归属月份，适配补录历史调整场景
- 绩效调整事件同月多条取序列号最大的值，其他类型事件按规则累加

##### 4.5.2 月度工资核算
- 支持按月份、人员范围筛选核算
- 前置校验：对应月份考勤未核算的人员，自动跳过并提示
- 读取月度出勤工资 + 职务快照 + 年假权益事件 + 工资事件，聚合计算最终工资
- 每次核算生成独立版本记录，留存完整核算快照，同步更新工资总表最新数据
- 数据变动检测：考勤工资重算、工资事件变动、职务事件变动、年假权益事件变动均会标记结果为「数据已变动」
- 无强制锁定，仅通过状态提示引导用户重新核算；导出时做二次确认提醒

##### 4.5.3 版本历史与追溯
- 支持查看单人员单月份所有核算版本，展示版本号、核算时间、操作人、实发工资
- 支持相邻版本一键对比，高亮展示变动薪资项与变动金额
- 支持从工资汇总行下钻查看薪资构成明细
- 出勤工资、加班工资可追溯到每日明细及对应原始考勤事件
- 未休年假结转的工资可追溯到假期余额及对应额度事件
- 绩效、补贴、代扣可追溯到对应职务时段标准
- 调整项可追溯到每一笔工资事件

#### 4.6 文件管理
- 全局文件列表展示，支持按名称、类型、上传时间筛选
- 文件上传：文件本体写入本地文件系统，自动生成元数据记录
- 支持一个文件关联多个业务对象
- 文件删除仅软删除元数据，本地文件保留；恢复仅恢复元数据与关联记录
- 所有操作计入审计

#### 4.7 操作审计
- 全量操作日志展示，支持按操作人、操作类型、对象类型、时间范围筛选
- 每条日志展示操作人、操作时间、操作类型、操作对象名称
- 详情页左右两栏布局，分别展示操作前、操作后数据快照，自动过滤技术字段
- 仅支持查询和导出，无任何修改、删除接口，数据永久留存

#### 4.8 用户与权限
- 用户管理：用户增删改、重置密码、启用禁用、绑定人员、分配角色
- 角色管理：角色增删改、权限分配，支持自定义角色
- 权限粒度：模块级读/写/删/导出四级权限，覆盖所有业务模块
- 前后端双重校验，前端隐藏不绕过后端权限校验
- 默认初始化超级管理员账号，首次登录强制修改密码
- 默认超级管理员角色禁止删除、禁止修改权限

#### 4.9 系统配置
- 分组展示所有全局配置项，按值类型自动渲染表单控件
- 配置修改自动刷新内存缓存，实时生效
- 支持配置项扩展，新增配置仅需插入初始数据即可

---

### 五、后端架构设计
#### 5.1 架构模式
1. **model层**：定义数据表结构体、GORM标签、业务常量、枚举校验规则
2. **dao层**：封装数据库读写操作，不包含业务逻辑
3. **service层**：核心业务逻辑、事务控制、数据校验；负责事件序列号生成、投影重建、核算运算
4. **handler层**：HTTP请求处理、参数校验、调用Service、封装响应
5. **router层**：路由注册、中间件绑定

#### 5.2 投影统一设计规范
所有投影层严格遵循统一规则：
1. **统一字段**：必须包含`last_calc_at`字段，记录最后核算时间
2. **统一状态判断**：提供统一方法判断投影是否过期（下层数据更新时间 > 本层last_calc_at）
3. **统一重建能力**：提供全量重建、按范围重建方法，保证幂等
4. **统一触发机制**：
   - 基础投影：下层事件变动后自动触发重算
   - 中上层投影：提供手动核算接口，支持批量
5. **统一追溯能力**：提供明细查询接口，支持从投影下钻到原始事件
6. **统一覆盖范围**：职务快照、日考勤记工时、年假余额、调休余额全部纳入基础投影统一管理

#### 5.3 核心架构特性
- **统一响应格式**：所有接口返回标准结构 `{code, msg, data}`
- **全局错误处理**：中间件统一捕获异常，返回友好提示，不暴露内部细节
- **事务保证**：所有多表写操作包裹事务，失败自动回滚；事件写入与序列号生成在同一事务内完成；投影重建采用「生成新数据→事务替换旧数据」的安全模式
- **配置热加载**：系统配置修改后自动刷新内存缓存，无需重启
- **审计全拦截**：单表CRUD由全局中间件自动审计；批量、跨表操作在Service层显式调用审计工具
- **权限双重校验**：前后端双重权限校验，前端隐藏不绕过后端校验
- **数据兜底校验**：所有枚举字段配置数据库CHECK约束，杜绝非法值入库

---

## 第二部分 前端开发落地指南
### 六、整体架构与核心规范
#### 6.1 核心设计原则
1. **模块与后端一一对应**：前后端模块边界、命名完全对齐，禁止跨模块乱写逻辑
2. **极简依赖原则**：仅使用指定依赖，能用Element Plus原生实现的不装第三方组件
3. **无ID展示铁律**：所有用户可见区域只展示业务名称，数据库ID全程在内存中传递；审计详情自动过滤技术字段
4. **权限驱动渲染**：菜单、按钮完全由权限动态控制，无权限则隐藏DOM
5. **单文件部署适配**：全程适配Hash路由、相对路径静态资源
6. **单位统一展示**：考勤类数据默认以天为单位展示（保留2位小数），内部交互用小时，换算标准8小时=1天
7. **投影状态统一**：所有投影层页面统一状态标签样式与交互，过期数据统一提示

#### 6.2 技术栈与依赖边界
| 分类 | 技术选型 | 用途说明 |
|------|----------|----------|
| 核心框架 | Vue 3 + TypeScript | 组合式API开发 |
| 构建工具 | Vite + Bun | 构建与包管理 |
| UI组件库 | Element Plus + @element-plus/icons-vue | 全部交互组件与官方图标 |
| 状态管理 | Pinia | 仅管理用户、权限、配置三类全局状态 |
| 请求库 | Axios | 统一请求封装与拦截 |
| 路由 | Vue Router | Hash模式 |
| 日期工具 | dayjs | 日期格式化与计算 |
| 样式 | SCSS | 少量全局样式 |

---

### 八、核心基础层落地规范
#### 8.1 路由设计
- 强制使用Hash模式，采用嵌套路由结构，外层为全局Layout，内层为业务页面
- 每个路由的meta字段必须包含`title`（页面标题）、`permissionKey`（对应权限标识）
- 采用「前端全量定义路由 + 权限过滤菜单 + 路由守卫校验」方案，无权限路由不渲染菜单DOM
- 保留404（路由不存在）、403（无权限访问）两个极简错误页面，均带返回首页按钮

#### 8.2 状态管理（Pinia）
- **user store**：存储用户基本信息、Token、首次登录标记；仅Token持久化至localStorage，用户信息不做本地缓存
- **permission store**：存储权限标识数组、侧边栏菜单列表，提供统一的`hasPermission(key)`权限判断方法
- **config store**：存储全局系统配置键值对，与后端系统配置模块一一对应
- 刷新页面后必须重新从后端拉取用户信息与权限列表，禁止复用本地缓存的权限与用户数据，保证权限实时生效

#### 8.3 请求封装（Axios）
- 基础URL规则：开发环境通过Vite代理转发，生产环境使用相对路径`/api`
- 请求拦截器：自动从Pinia获取Token，添加至`Authorization: Bearer xxx`请求头
- 响应拦截器统一处理：`code=0`时直接返回data；非0时通过ElMessage弹出错误提示并抛出异常；401状态清除Token并跳转登录页，403状态提示「无操作权限」
- 绝对禁止将后端错误堆栈、SQL异常等技术细节直接暴露给用户，统一返回友好业务提示

#### 8.4 权限三级控制
- 菜单级：通过权限列表动态过滤侧边栏菜单，同时路由守卫做访问兜底校验，无权限则不可见、不可访问
- 按钮级：通过`v-permission`全局指令或`hasPermission`方法判断，无权限直接移除DOM元素，不做置灰处理
- 接口级：后端接口独立校验权限，前端隐藏仅为交互体验优化，不替代后端安全校验

#### 8.5 首次登录强制改密
- 登录成功后检测到`is_first_login=true`，直接弹出修改密码弹窗，弹窗不可关闭、不可通过点击遮罩关闭
- 校验规则：新密码不能与初始默认密码相同
- 密码修改成功后才可进入系统主页面

---

### 第九章 通用组件与交互规范
#### 9.1 全局布局
采用标准中后台三栏布局，所有业务页面统一嵌入主内容区：
- 左侧侧边栏：垂直导航菜单，支持折叠，菜单图标统一使用 Element Plus 官方图标
- 顶部导航栏：左侧展示系统名称，右侧为用户下拉菜单，固定包含「个人信息、修改密码、退出登录」三项
- 主内容区：统一内边距，所有业务页面内容均在此区域内渲染

#### 9.2 ProTable 通用列表组件
所有业务列表页统一复用该组件，固定四区结构：顶部搜索筛选区、操作按钮区、数据表格区、底部分页区。
核心交互规则：
1. **搜索规则**：点击「搜索」按钮触发查询，输入过程不实时搜索；点击「重置」清空所有筛选条件并自动触发一次查询，回到默认列表
2. **分页约定**：统一分页参数为 `pageNum`（从1开始）、`pageSize`，接口响应统一返回 `{ list: [], total: number }` 格式
3. **多选列**：默认关闭，仅存在批量操作时通过配置开启
4. **加载与空状态**：遵循全局反馈规范

#### 9.3 ProFormDialog 通用弹窗表单
所有新增、编辑场景统一复用该组件，支持新增/编辑双模式。
核心交互规则：
1. **打开逻辑**：先重置表单至初始值、清除所有校验状态；编辑模式在重置完成后再回填数据
2. **关闭逻辑**：销毁表单实例，清除残留数据与校验状态，避免数据串扰
3. **提交逻辑**：自带「确定」「取消」按钮，提交时自动执行表单校验，调用传入的提交方法；提交成功后自动关闭弹窗并触发父列表刷新
4. **加载状态**：提交过程展示loading，遵循全局反馈规范

#### 9.4 NameSelect 名称选择器
所有人员、公司等业务对象选择场景统一使用，严格落实「无ID展示铁律」。
核心规则：
1. **展示规则**：选项仅展示业务名称，数据库ID仅在组件内部绑定传递，用户全程不可见
2. **绑定规则**：v-model 绑定ID值，提交时直接传给后端，前端不做名称到ID的转换
3. **搜索规则**：500条以内全量加载，前端模糊匹配名称；超过阈值自动切换为远程搜索模式

#### 9.5 投影状态统一规范
所有核算类、投影类页面的状态标签样式与语义全局统一，共三类状态：
- 灰色标签：未核算，对应投影数据未生成
- 绿色标签：已核算，对应当前投影数据与下层数据源完全一致
- 橙色标签：数据已变动，对应下层原始数据已更新，当前投影结果已过期，建议重新核算

#### 9.6 回收站交互规范
所有业务模块回收站交互全局统一：
1. **统一入口**：列表页右上角「回收站」按钮，点击以抽屉形式展示当前模块已软删除的数据
2. **统一操作**：仅支持「恢复」操作，恢复前弹出二次确认
3. **强制约束**：所有业务模块禁止提供「彻底删除」功能，所有删除均为软删除，数据永久可恢复

#### 9.7 全局反馈规范
全局交互体验统一，禁止各页面自定义反馈组件：
1. **普通操作提示**：统一使用 `ElMessage`，成功显示绿色提示、失败显示红色提示
2. **危险操作二次确认**：删除、恢复、覆盖核算、反结账、危险配置修改等风险操作，必须使用 `ElMessageBox.confirm` 弹出确认框，用户确认后才可执行
3. **加载状态**：表格加载、表单提交、弹窗打开过程中必须显示对应loading，禁止无加载态的空白等待
4. **空数据状态**：列表无数据时必须使用 Element Plus 空状态组件，禁止空白页面

#### 9.8 数据展示统一规范
所有业务页面数据格式全局统一：
1. **关联名称**：所有列表的关联对象名称由后端接口直接返回，前端不做跨表关联查询，不自行拼接名称
2. **日期格式**：日期统一为 `YYYY-MM-DD`，月份统一为 `YYYY-MM`，完整时间统一为 `YYYY-MM-DD HH:mm:ss`
3. **考勤单位**：列表、表单默认以天为单位展示（保留2位小数），接口交互统一使用小时；换算标准固定为**8小时=1天**，由工具函数统一转换
4. **金额格式**：所有金额数值统一保留2位小数展示
5. **技术字段屏蔽**：审计详情、数据快照等场景自动过滤ID、时间戳、软删除标记等技术字段，仅展示业务字段

---

### 第十章 业务模块分模块落地细节
#### 10.1 人员管理模块
**页面组成**：主列表页 + 新增/编辑弹窗 + 详情弹窗（标签页布局）
**搜索筛选**：姓名（模糊）、身份证号（模糊）、考勤组（下拉）、在职状态（下拉）
**核心交互**：
1. 详情弹窗标签页包含：基础信息、职务信息、关联文件
2. 多值字段在详情弹窗内独立管理，支持单独增删改
3. 附件标签页支持直接上传文件并自动关联当前人员
4. 表格操作列固定为：查看详情、编辑、职务事件、考勤记录、工资记录、删除
5. 点击「职务事件/考勤记录/工资记录」直接跳转对应模块并自动筛选当前人员

#### 10.2 公司管理模块
**页面组成**：列表页 + 新增/编辑弹窗 + 附件管理
**搜索筛选**：公司名称（模糊）、统一社会信用代码（模糊）
**核心交互**：
1. 详情弹窗标签页包含：基础信息、关联文件
2. 多值字段在详情弹窗内独立管理，支持单独增删改
3. 附件标签页支持直接上传文件并自动关联当前公司
4. 表格操作列固定为：查看详情、编辑、附件管理、删除

#### 10.3 职务事件管理模块
**页面组成**：事件列表页 + 新增/编辑弹窗
**搜索筛选**：人员选择、生效日期范围、事件名称
**核心交互**：
1. 新增/编辑采用「复选框驱动动态表单」：仅勾选的字段展示输入框，未勾选字段不提交
2. 编辑事件时，已变更字段默认勾选并回填数值；取消勾选后提交时该字段置为NULL
3. 表格增加「变更字段」列，展示本次事件涉及变动的字段名称

#### 10.4 考勤管理模块
##### 页面1：考勤事件管理
**搜索筛选**：人员选择、日期范围、事件类型、子类型
**核心操作**：单条新增、批量新增、导出
**核心交互**：
1. 事件类型与子类型两级联动，选择一级类型后，二级下拉仅展示对应子项
2. 批量新增：分两步操作，第一步选择人员（支持多选、按考勤组筛选），第二步选择起止日期、事件类型、每日时长。填写事件信息提交后，后端自动拆分为单日单条事件并分配连续序列号。添加完成后展示添加成功/失败数量。
3. 时长输入默认按小时录入
4. 打卡时间为可选备注字段，不参与任何计算，仅做留痕
5. 选择年假、调休类型时，自动展示该人员当前可用余额

##### 页面2：日记工时查询
**搜索筛选**：人员选择、日期范围
**核心交互**：
1. 表格列：日期、人员姓名、记出勤天数、工作日加班时长、节假日加班时长、违纪次数、是否有事假、状态
2. 支持查看当日对应的所有原始考勤事件明细
3. 支持按筛选范围导出

##### 页面3：月度考勤工资核算
**搜索筛选**：月份范围、人员选择、考勤组
**核心操作**：核算、批量核算、导出
**核心交互**：
1. 表格列：月份、人员姓名、出勤工资、工作日加班工资、节假日加班工资、全勤奖、状态
2. 支持点击行下钻查看每日明细，再进一步追溯到原始考勤事件
3. 核算前自动过滤无在职记录的人员，核算完成后展示成功/失败数量。
4. 导出时若存在橙色状态数据，弹出二次确认提示

#### 10.5 假期权益管理模块
##### 10.5.1 年假
###### 页面1：年假事件流水(调取考勤事件中的休年假事件，和年假权益事件中配发、结转、人工调整事件)
**搜索筛选**：人员选择、日期范围
**核心交互**：
1. 表格列：日期、人员姓名、事件类型、变动时长、事件来源、备注
2. 系统周年类事件置灰显示，不提供编辑、删除操作
3. 支持人工录入额度调整事件、修改已有额度调整事件

###### 页面2：余额查询(调取年假余额时段快照)
**搜索筛选**：人员选择
**核心交互**：
1. 表格列：人员姓名、当前可用额度
2. 支持点击行下钻查看额度构成明细：累计配发、累计已休、累计调整、累计结转
3. 数据为自动投影生成，仅支持查询，无编辑入口

###### 页面3：周年结转管理
**搜索筛选**：批次号、业务周期、操作人
**核心操作**：批量结转、反结账、查看明细
**核心交互**：
1. 顶部「年假周年批量结转」按钮，选择目标月份后执行批量处理
2. 批次列表展示批次号、业务周期、操作人、操作时间、处理人数
3. 已生效批次支持「反结账」操作，点击二次确认后执行冲销
4. 点击批次可查看该批次下的所有额度事件明细

##### 10.5.2 调休
###### 页面1：调休事件流水(调取考勤事件中的补班和调休事件)
**搜索筛选**：人员选择、日期范围
**核心交互**：
1. 表格列：日期、人员姓名、事件类型、变动时长、事件来源、备注
2. 支持人工录入考勤事件、修改已有考勤事件

###### 页面2：余额查询(调取调休余额时段快照)
**搜索筛选**：人员选择
**核心交互**：
1. 表格列：人员姓名、当前可用额度（实时余额=累计补班-累计调休）
2. 支持点击行下钻查看额度构成明细：累计补班、累计调休
3. 数据从事件自动投影生成，仅支持查询，无编辑入口

#### 10.6 工资管理模块
##### 页面1：工资事件管理
**搜索筛选**：人员选择、归属月份、事件类型
**核心交互**：
1. 绩效调整类型事件，金额字段输入绩效系数，前端校验范围0-2
2. 事件归属月份可手动选择，支持补录历史月份调整项

##### 页面2：月度工资汇总
**搜索筛选**：月份范围、人员选择、考勤组
**核心操作**：核算、批量核算、导出、版本历史
**核心交互**：
1. 表格展示薪资全量明细项与实发工资、状态
2. 前置校验：对应月份未完成考勤工资核算的人员自动跳过并提示
3. 支持下钻查看薪资构成明细，逐层追溯到原始数据
4. 支持查看单人员当月所有核算版本，进行版本对比
5. 导出时若存在数据变动记录，弹出二次确认提示

#### 10.7 文件管理模块
**页面组成**：全局文件列表 + 上传弹窗 + 关联调整弹窗 + 回收站抽屉
**搜索筛选**：文件名（模糊）、文件类型、上传时间范围
**核心交互**：
1. 全局上传不绑定业务对象，上传后可手动调整关联关系
2. 支持一个文件关联多个业务对象，关联调整弹窗可选择关联类型与目标
3. 表格操作列固定为：下载、修改关联、删除

#### 10.8 审计日志模块
**页面组成**：日志列表页 + 详情弹窗
**搜索筛选**：操作人、操作类型、对象类型、时间范围
**核心交互**：
1. 列表展示操作人、操作时间、操作类型、操作对象名称
2. 详情弹窗左右两栏展示操作前后数据快照
3. 仅支持查询、导出两个操作，无修改类按钮

#### 10.9 用户与权限模块
**页面组成**：用户管理页 + 角色管理页 + 权限分配弹窗
**核心交互**：
1. **用户管理**：支持用户新增、编辑、重置密码、分配角色、启用/禁用；重置密码需二次确认，重置成功后弹出提示告知默认密码；禁用状态的账号无法登录系统
2. **角色管理**：支持角色新增、编辑、分配权限；权限采用树形结构，按业务模块分组展示

#### 10.10 系统配置模块
**页面组成**：单页配置列表，按模块分组展示
**核心交互**：
1. 根据配置项的值类型自动渲染对应控件：输入框、数字框、开关、下拉选择
2. 修改配置即时保存，后端自动刷新内存缓存，实时生效

---

## 第三部分 部署与运维
### 十四、部署与运行
#### 14.1 部署方式
- 核心服务：单个二进制可执行文件，嵌入前端静态资源
- 数据存储：SQLite数据库文件 + upload文件目录，默认同级目录
- 无需额外部署Nginx、数据库、中间件

#### 14.2 初始化流程
- 首次启动自动迁移表结构、插入默认配置与枚举约束
- 自动创建超级管理员admin/admin123，标记首次登录
- 启动即可访问使用

#### 14.3 数据备份与恢复
- 备份：同时备份数据库文件与upload目录
- 恢复：替换数据库文件与upload目录即可

---
### 十五、扩展预留设计
#### 15.1 业务功能扩展
1. **钉钉打卡导入**：流程框架已预留，后续补充解析逻辑即可
2. **数据权限扩展**：预留扩展位，可实现不同角色查看不同范围数据
3. **定时任务扩展**：预留接入位，可扩展自动核算、自动备份、自动周年处理
4. **数据库升级**：架构抽象完善，规模扩大后可平滑迁移至MySQL