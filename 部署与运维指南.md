# 部署与运维指南

## 系统概述

Probig 是面向中小企业的轻量人事与行政管理系统，采用 Go + Vue3 全栈架构，编译为**单个二进制文件**，无需额外安装数据库、Web 服务器或运行时环境。

## 环境要求

| 项目 | 最低要求 |
|------|---------|
| 操作系统 | Linux / Windows / macOS |
| CPU | 1 核 |
| 内存 | 128 MB |
| 磁盘 | 100 MB（含数据库和上传文件空间） |
| Go | 1.21+（仅编译时需要） |
| Bun | 1.0+（仅前端构建时需要） |

## 快速开始

### 1. 编译构建

```bash
# 克隆项目
git clone <repo-url> probig
cd probig

# 构建前端（生成 server/cmd/server/static/）
cd web && bun install && bun run build && cd ..

# 构建后端（生成 server/probig-server）
cd server && go build -o probig-server ./cmd/server/ && cd ..

# 拷贝二进制到根目录
cp server/probig-server .

# 拷贝配置文件（可选，使用默认值也可直接运行）
cp server/config/config.yaml .
```

### 2. 启动

```bash
./probig-server
```

首次启动自动完成：
- SQLite 数据库创建（`data/probig.db`）
- 表结构迁移
- 系统配置种子（7 项）
- 超级管理员账号创建
- 权限体系初始化

### 3. 访问

浏览器打开 `http://localhost:8080`

默认账号：
| 用户名 | 密码 | 备注 |
|--------|------|------|
| admin | admin123 | 首次登录强制改密 |

### 4. 自定义端口

修改 `config/config.yaml`：
```yaml
server:
  port: 9090
```

---

## 运行时目录结构

```
probig-server        ← 二进制文件
config/
  config.yaml        ← 配置文件（可选）
data/
  probig.db          ← SQLite 数据库
  uploads/           ← 上传文件存储
  probig.log         ← 日志文件（可选）
```

`data/` 目录首次启动时自动创建。

---

## 配置文件说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `server.port` | 8080 | HTTP 监听端口 |
| `server.mode` | debug | debug/release |
| `database.path` | ./data/probig.db | 数据库文件路径 |
| `jwt.secret` | probig-jwt-secret... | JWT 签名密钥（生产环境请修改） |
| `log.level` | info | 日志级别 |
| `log.file` | ./data/probig.log | 日志文件路径 |

---

## 数据备份

### 完整备份

```bash
# 停止服务
# 备份整个 data 目录
tar -czf probig-backup-$(date +%Y%m%d).tar.gz data/
```

### 仅备份数据库

```bash
cp data/probig.db data/probig.db.bak
```

### 恢复

```bash
# 停止服务
rm -rf data/
tar -xzf probig-backup-20260101.tar.gz
# 重新启动
./probig-server
```

---

## 文件存储

所有上传文件存储在 `data/uploads/` 目录。备份数据库时需同步备份此目录。文件与业务实体通过数据库中的 `file_relations` 表关联。

---

## 系统配置管理

7 项可配置参数通过系统后台「系统配置」页面管理：

| 配置项 | 说明 |
|--------|------|
| 计薪小时基准 | 每日标准计薪小时数（默认 8） |
| 病假系数 | 病假折算记出勤工时系数（默认 0.8） |
| 工作日加班系数 | 工作日加班工资倍数（默认 1.5） |
| 节假日加班系数 | 节假日加班工资倍数（默认 2.0） |
| 全勤奖日标准 | 全勤奖每日金额（默认 50） |
| 高温补贴发放月份 | 多选月份（默认 6/7/8/9 月） |
| 年假年度额度 | 每年标准年假小时数（默认 40） |

所有配置修改即时生效，无需重启。

---

## 常用运维命令

```bash
# 后台运行
nohup ./probig-server > probig.log 2>&1 &

# 查看进程
ps aux | grep probig-server

# 停止服务
pkill probig-server

# 查看日志
tail -f data/probig.log
```

---

## 性能参考

| 场景 | 指标 |
|------|------|
| 内存占用（空载） | ~30 MB |
| 内存占用（100人、1年数据） | ~80 MB |
| 数据库大小（100人、1年数据） | ~10 MB |
| 启动时间 | < 2 秒 |
| API 响应时间 | < 50 ms |

---

## 升级步骤

```bash
# 1. 停止服务
pkill probig-server

# 2. 备份数据（重要！）
tar -czf probig-backup-$(date +%Y%m%d).tar.gz data/

# 3. 替换二进制文件
cp /path/to/new/probig-server .

# 4. 重新启动
./probig-server

# 5. 验证
curl http://localhost:8080/api/health
```

首次启动时会自动执行数据库迁移，新增表/字段自动创建，无需手动操作。

---

## 故障排查

| 现象 | 可能原因 | 解决 |
|------|---------|------|
| 启动失败 `bind: address already in use` | 端口被占用 | 修改 config.yaml 端口或 kill 旧进程 |
| 页面空白 | 静态资源未嵌入 | 确保前端已构建到 `cmd/server/static/` |
| 数据库损坏 | 异常断电 | 用备份的 `probig.db` 替换 |
| 上传失败 | uploads 目录权限不足 | `chmod 755 data/uploads` |
| 磁盘空间不足 | 上传文件过多 | 清理 data/uploads 或扩容磁盘 |

---

## 安全建议

1. **修改默认密码**：首次登录后立即修改 admin 密码
2. **修改 JWT Secret**：编辑 `config.yaml` 中 `jwt.secret` 为随机字符串
3. **反向代理**：生产环境中通过 Nginx/Caddy 反代，配置 HTTPS
4. **防火墙**：限制 8080 端口仅内网可访问
5. **定期备份**：建议每日自动备份 `data/` 目录
