# 项目开发记录

## 项目概述

**Probig** — 企业人事与行政管理系统。面向中小企业的轻量生产级软件，覆盖人员管理、公司管理、考勤、假期权益、薪资核算、文件管理、操作审计全流程。

**技术栈**: Go + Gin + GORM + SQLite（后端），Vue3 + TypeScript + Element Plus + Pinia（前端），单二进制部署。

**开发周期**: 12 个阶段 + 3 轮重构（P0/P1/P2），约 28,000 行代码，220+ API 端点，35+ 数据库表。

---

## 一、关键架构决策

### 1.1 事件驱动 + 多层投影模型 (L0→L1→L2→L3)

这是整个系统最核心的架构设计，灵感来自 Event Sourcing 和 CQRS。

```
L0 事件源层: position_events, attendance_events, annual_leave_events, salary_events
    ↓ 自动触发
L1 基础投影层: position_snapshots, attendance_daily_projection, annual_leave_balance, leave_in_lieu_balance
    ↓ 手动触发
L2 业务核算层: attendance_calculation_monthly
    ↓ 手动触发
L3 结果汇总层: salary_summaries + salary_summary_versions
```

**评估**: ★★★★★ 架构清晰分层，每一层职责单一。下层数据变更 → 上层自动/手动重建，保证全链路可追溯。该模式在今年假余额、调休余额、薪资核算中复用了三次，证明其通用性。

### 1.2 统一序列号生成模式

所有 4 种事件表（position_events, attendance_events, annual_leave_account_events, salary_events）使用完全相同的 seq 生成逻辑：

```go
// 统一模式，在所有事件 Service 中一字不差地复用
tx.Unscoped().Model(&Model{}).Where("person_id = ?", pid).
    Select("COALESCE(MAX(seq), 0)").Scan(&maxSeq)
event.Seq = maxSeq + 1
```

配合部分唯一索引 `(person_id, seq) WHERE deleted_at IS NULL`，实现了 seq 永久消耗、永不回收。删除事件 → 新建事件 → 恢复旧事件三者互不冲突。

**评估**: ★★★★★ 从第一个事件表（position_events）开始就确立了该模式，后续 3 个事件表 100% 复用，未出现任何 seq 相关的 bug。

### 1.3 时段快照投影算法

position_snapshots、annual_leave_balance_snapshots、leave_in_lieu_balance_snapshots 三个投影表共用一个时段算法：

```
1. DELETE 现有快照
2. 收集所有事件，按日期+seq 排序
3. 遍历事件链 → 字段叠加 → 遇变更生成新时段
4. 前一时段 end = 新事件日期 - 1天
5. 最后一段 end = 9999-12-31
6. 同日事件: end < start → 跳过无效时段
```

**评估**: ★★★★★ 三层投影共享完全相同的时段生成逻辑。引入 DateOnly 类型后，日期运算与 JSON 序列化统一为 `YYYY-MM-DD` 格式，消除了所有 RFC3339 兼容问题。

### 1.4 前端通用组件体系

| 组件 | 复用次数 | 职责 |
|------|---------|------|
| `ProTable` | 20+ 页面 | 搜索/筛选/分页/加载/空状态 |
| `ProFormDialog` | 10+ 页面 | 新增/编辑双模式弹窗 |
| `NameSelect` | 15+ 页面 | 名称选择器，v-model=ID，display=name |
| `FileAttachPanel` | 7 页面 | 文件上传/关联/移除 |
| `RecycleBinDrawer` | 10+ 页面 | 软删除/恢复 |
| `StatusTag` | 3+ 页面 | 投影状态标签 |

**评估**: ★★★★☆ ProTable 的 SearchField 类型系统（input/select/date-range/person-select/month）经过三次迭代逐步完善，目前已覆盖所有筛选场景。

### 1.5 GORM Hooks 审计体系

```go
db.Callback().Create().After("gorm:after_create").Register("audit:create", hook)
db.Callback().Update().After("gorm:after_update").Register("audit:update", hook)
db.Callback().Delete().After("gorm:after_delete").Register("audit:delete", hook)
```

三个 hook 自动记录所有 L0 表的增删改操作。操作前/后快照存为 JSON，排除投影表（L1/L2/L3）及辅助表（files、file_relations、sys_batches）。

**评估**: ★★★★☆ GORM Callback 是唯一侵入基础设施的代码，但只需在 `initSystem()` 中注册一次，后续 CRUD 零改动自动受益。

### 1.6 Sentinel Error 模式

跳过来完成考勤核算的人员时，最初使用 `err.Error()[:6] == "未完成月度考勤核算"` 字符串匹配。后重构为标准 sentinel error：

```go
var ErrAttendanceNotCalculated = errors.New("未完成月度考勤核算，请先进行考勤核算")
// CalculateSalary 中: return fmt.Errorf("%w", ErrAttendanceNotCalculated)
// CalculateSalaryBatch 中: if errors.Is(err, ErrAttendanceNotCalculated) { skip++ }
```

**评估**: ★★★★★ 符合 Go 官方 error 处理最佳实践，类型安全、无语言耦合。

### 1.7 配置与路径统一

配置文件 `config.yaml` 始终与二进制文件同目录。`config.LoadConfig()` 只查 `GetExeDir()/config.yaml` 一个路径。`config.ResolvePath()` 将所有相对路径解析到二进制所在目录。

首次启动无配置文件时自动使用默认值生成 `config.yaml`，创建数据库和上传目录。开发环境在 `server/dist/config.yaml` 预设 `mode: debug`，开发流程与生产完全对齐。

**评估**: ★★★★★ 消除了先前的多路径查找和 `baseDir` 状态机。config.go 从 143 行缩减到 126 行，逻辑截半。不论从哪个目录运行二进制，持久化数据始终落在二进制同级目录。

### 1.8 级联软删除

Person 和 Company 的主表软删除同步级联所有关联子项：Person 删除时级联软删除 phones/emails/bankCards/4 类事件源/file_relations；恢复时同步恢复全部。Company 删除/恢复时级联 file_relations。File 删除仅软删除元数据；恢复时同步恢复关联记录。全部操作包裹在事务中。投影层不参与级联，重建时自动过滤已软删除的事件源。

**评估**: ★★★★★ 消除了"孤儿关联"问题——之前 file_relations 不被级联删除导致 `CleanOrphanFiles` 永远无法清理有历史关联的文件。级联后 `resolveTargetName` 等双向查询自动利用 GORM 的 `deleted_at IS NULL` 过滤，无需额外判断。

### 1.9 数据变动检测

月度考勤核算和工资汇总列表自动计算 stale 状态，后端在接口返回中附带 `status` 字段（`calculated` 或 `data_changed`），前端用 `StatusTag` 组件渲染绿色/橙色标签。工资汇总的 staleness 判断按企划的 4 条件检测（考勤核算 / 职务快照 / 工资事件 / 年假权益事件，任一 `last_calc_at` 大于汇总的 `last_calc_at` 即标记为变动）。

**评估**: ★★★★★ L0→L3 投影闭环的关键环节。此前 StatusTag 组件定义了三种状态但完全未被使用。

### 1.10 工资核算公式重构

原始公式使用 `totalDays + allowanceRatio + isActive` 三段式，存在两个核心 bug：(1) `isActive` 恒为 true 导致 `allowanceRatio` 永不触发，离职月全额发放补贴；(2) 离职后时段快照继承在职期补贴值，虚增加权分子。

重构后的最终公式：

```
wXxx        = Σ(snapshotValue × activeSegDays)           // 只对 IsActive=true 的快照加权
activeDays   = Σ(activeSegDays)
attendanceDays = totalWorkHours / 8                       // 实际出勤天数
isFullMonth   = (activeDays == totalCalendarDays)

补贴（8 项）:
  isFullMonth  → wXxx / activeDays                        (直取定额)
  !isFullMonth → (wXxx / activeDays) × (attendanceDays / salaryDays)  (按出勤折算)

绩效:
  isFullMonth  → (wPerfBase / activeDays) × 系数           (直取定额)
  !isFullMonth → (wPerfBase / activeDays) × (attendanceDays / salaryDays) × 系数

代扣:
  ss/hfDeduct  → wXxx / activeDays                         (永不折算)
```

考勤核算的 `weightedBase` 同步修正：循环拆为两段，第一段遍历全部快照做 `hasAttendanceBonus` 判定，第二段仅遍历 `IsActive=true` 的快照做加权求和。

**评估**: ★★★★★ 31 个测试覆盖全月/离职/违纪/事假/病假/年假/绩效系数/调整项/高温月/调薪/综合边界等 11 个场景。

### 1.11 事件导出 + 年假合并 + 追溯 API

4 种事件（职务、考勤、年假权益、工资）各新增 `GET /export` 端点，输出 Excel 文件。年假事件列表合并展示考勤事件中的年假休假记录（`source="attendance"`）。新增余额明细 API（年假/调休构成下的累计配发/已休/结转/调整），新增工资全链路追溯 API（summary → calc → daily projections → raw events → snapshots）。

**评估**: ★★★★☆ 补齐了企划中预留但此前缺失的导出和追溯功能。

---

## 二、关键 Bug 及处理

### 2.1 浏览器窗口无法最小化/持续闪动

**根因**: ProTable 模板中遗留了字面量 `\n` 字符（非换行符），Vue 模板编译失败 → 渲染死循环 → 浏览器检测到持续活动 → 任务栏呼吸式跳动。在浏览器多标签模式下尤为明显。

**处理**: `grep` 全量扫描 `.vue` 文件定位到 `\n` 字面量 → 替换为真正换行符。

**分类**: 脚本引入的人为错误，非架构缺陷。

---

### 2.2 SQLite 驱动 CGO 依赖

**根因**: `mattn/go-sqlite3` 需要 CGO，目标机器无 gcc。

**处理**: 替换为 `glebarez/sqlite`（纯 Go 实现），一行 import 改动解决。该驱动基于 `modernc.org/sqlite`。

**分类**: 环境适配，非架构缺陷。

---

### 2.3 软删除不生效（`*time.Time` vs `gorm.DeletedAt`）

**根因**: User 和 Role 模型的 `DeletedAt` 字段定义为 `*time.Time`。GORM v2 需要 `gorm.DeletedAt` 类型才能启用软删除机制。`dao.DB.Delete()` 在 `*time.Time` 上执行的是**物理删除**而非软删除。

**处理**: 将所有事件模型的 `DeletedAt *time.Time` 改为 `DeletedAt gorm.DeletedAt`。

**分类**: 框架使用错误，应在模型定义之初就使用正确类型。后续所有新增模型均统一用 `gorm.DeletedAt`。

---

### 2.4 日期格式导致编辑回填出错

**根因**: Go `time.Time` JSON 默认序列化为 RFC3339（`2025-03-01T00:00:00Z`），前端 `el-date-picker`（`value-format="YYYY-MM-DD"`）无法识别，字段回填为空 → 提交时后端收到零值 `0001-01-01`。

**处理**: 创建 `utils.DateOnly` 类型，实现 `MarshalJSON`/`UnmarshalJSON`/`Scan`/`Value` 四接口。JSON 序列化为 `"2025-03-01"`，GORM 数据库读写通过 `driver.Value` 接口透明转换。

**评估**: ★★★★★ 该类型被全部模型中的日期字段统一使用，一次定义永久受益。这是全项目中复用度最高的工具类型。

---

### 2.5 空字符串触发唯一索引冲突

**根因**: 部分唯一索引 `WHERE deleted_at IS NULL` 将空字符串 `""` 视为合法唯一值。当多个未填写 id_card 的人员同时存在时，`id_card = ''` 被索引视为重复，导致第 2 个人员创建失败。

**处理**: 部分索引改为 `WHERE deleted_at IS NULL AND id_card != ''`，同理 `credit_code != ''`。语义上，空串等价于「未填写」而非「唯一标识」。

**评估**: ★★★★★ 应用层（CreatePerson/RestorePerson）与数据库层逻辑一致。

---

### 2.6 is_active 在职务快照中全为 false

**根因**: `RebuildPositionSnapshots` 的 `apply()` 函数处理所有字段变更，但 `is_active` 的赋值在事件遍历循环**之外**的后置修正中。中间时段快照使用的 `prev` 状态中 `is_active` 永远是零值 `false`。

**处理**: 将 `is_active` 的赋值移入 `apply()` 函数内部，随 `EntryDate`/`LeaveDate` 事件即时流转。删除末尾的冗余循环。

**评估**: ★★★★☆ 逻辑 bug，修复后 `is_active` 与事件流同步。

---

### 2.7 中文错误消息字节切片导致 skip 逻辑失效

**根因**: `err.Error()[:6] == "未完成月度考勤核算"` — Go 的字符串索引操作在 []byte 层面，中文 UTF-8 编码每字 3 字节，`[:6]` 只取到前 2 个汉字 `"未完"`，不等于完整字符串。

**处理**: 重构为 sentinel error 模式（见 1.6）。

**分类**: 对 Go 字符串索引机制理解不深导致的反模式。重构后符合最佳实践。

---

### 2.8 打卡时间戳/违纪事件 hours 逻辑混乱

**根因**: 前端切换事件类型时未重置 `hours` 和 `punch_time`，旧值残留。后端也无兜底校验。

**处理**: 
- 前端 `onTypeChange()` 中：出勤/休假/加班类型 hours 默认 8，打卡时间戳/违纪 hours 默认 0
- 前端 `handleSubmit()`：打卡时间戳/违纪不发送 hours 字段
- 后端 `CreateAttendanceEvent` handler：event_type/sub_type 合法性校验
- 前端 ProTable 列：hours=0 且属于打卡时间戳/违纪时显示 `-`

**评估**: ★★★★☆ 前后端三层防线（前端表单/后端校验/展示层），逻辑完整。

---

### 2.9 修改考勤日期后旧日期留脏数据

**根因**: `UpdateAttendanceEvent` 只对旧日期（`existing.EventDate`）做投影重建，未对新日期做。

**处理**: 更新后同时重建新旧两个日期的日记工时投影。

**分类**: 边界情况遗漏，修复简单。

---

### 2.10 工资事件 `salary.delete` 权限缺失

**根因**: 种子数据中 `salary` 模块只有 `read/write/export` 权限，未包含 `delete`。Router 中 `DELETE /api/salary-events/:id` 需要 `salary.delete` 权限 → 超级管理员也无权删除。

**处理**: 种子数据补上 `"delete"`。

**分类**: 权限配置遗漏，非架构问题。

---

### 2.11 has_annual_leave / has_attendance_bonus 无法提交

**根因**: 职务事件表单中，这两个 bool 字段的 checkbox 只绑定了 `fieldFlags`（控制是否发送），没有设置 `eventForm` 的实际值。`handleSubmit` 中 `eventForm[f]` 为空字符串 → 发送 `null` → GORM skip → 数据库不更新。

**处理**: `handleSubmit` 中对 bool 字段特殊处理——当 `fieldFlags[f]` 为 true 时，强制 `data[f] = true`。

**分类**: Position events 的「只存变更字段」设计（NULL = 未变更）在 bool 字段上有先天不足——无法区分「不发送」和「发送 false」。当前处理是可在设计层面接受的折中。

---

### 2.12 下载文件返回 JSON 错误

**根因**: `DownloadFile` 用 `dao.DB.First()` 查文件（GORM 默认过滤软删除），文件被软删除后 → 返回 `utils.Error(c, "文件不存在")` → HTTP 200 + `{code:500}`。前端用 `responseType:'blob'` → 拦截器检测 blob → 跳过 JSON 解析 → 将错误 JSON 当文件下载。

**处理**: `DownloadFile` 改用 `Unscoped().First()` 且文件真不存在时返回 HTTP 404 状态码（非 200+JSON）。

**分类**: 响应格式不一致引发的连锁 bug。根治方式是区分「文件不存在」应发 404 而非 200+code:500。

---

### 2.13 NameSelect 收到 undefined 导致 Vue 警告 + 浏览器活跃

**根因**: ProTable 的 `initSearchForm()` 在 `onMounted` 中执行，Vue 模板在 mount 前即解析。此时 `searchForm` 是空 `{}`，`person-select` 字段的 key 不存在 → `searchForm[prop]` 返回 `undefined` → NameSelect 的 `modelValue` prop（类型 Number | Array | Null）收到 undefined → prop type warning → watch 循环 → 浏览器活跃。

**处理**: `initSearchForm()` 在 setup 阶段立即调用（`searchForm` 声明后），确保所有字段在首次渲染前已初始化为 `null`。

**分类**: Vue 3 Composition API 的时序问题，修复方案（setup 阶段初始化）是 Vue 官方最佳实践。

---

### 2.14 allowanceRatio 永不触发导致离职月全额发补贴

**根因**: `isActive` 变量在循环中被所有 `IsActive=true` 的快照置为 true 后永不回退。`if !isActive { allowanceRatio = ... }` 分支永远不执行，`allowanceRatio` 恒为 1.0。离职员工末月拿全额津贴和绩效，而非按比例折算。

**处理**: 删除 `isActive` 状态变量，改为 `isFullMonth = (activeDays == totalCalendarDays)` 二分支。全月在职直接 `wXxx/activeDays`（加权平均 = 月标准值），入职/离职月 `wXxx/calc.SalaryDays`（按计薪天数折算）。

**分类**: 逻辑 bug，公式基础概念混淆（日历天 vs 计薪天）。

---

### 2.15 离职后快照虚增补贴加权分子

**根因**: 职务快照的补贴字段从入职事件继承，离职事件仅设置 `LeaveDate` 不留空补贴值。`CalculateSalary` 中 `for _, s := range snapshots` 遍历全部快照（含离职后 `IsActive=false` 的时段），离职后虚假的补贴值被乘以时段天数累加到 `wPost` 等加权和中。加权分子虚高，且 `totalDays`（含离职后）作为分母时全月场景的分母被虚增。

**处理**: 循环中 `if !s.IsActive { continue }` 直接跳过非在职时段，`activeDays` 仅计在职日历日。

**分类**: 数据模型设计缺陷（快照继承）与业务逻辑（加权分母选择）叠加导致的复合 bug。修复后在 31 个测试中全部通过。

---

### 2.16 8 个模型的 CreatedAt/UpdatedAt 类型错误

**根因**: attendance_event/salary_event/annual_leave_account_event 等模型将 `CreatedAt`/`UpdatedAt` 定义为 `utils.DateOnly` 而非 `time.Time`。`DateOnly` 只有日期精度无时分秒，导致 GORM 的自动时间追踪丢失精度且与 GORM 标准行为不一致。

**处理**: 全部 8 个文件改为 `time.Time`，保留 `utils.DateOnly` 仅用于业务日期字段（`EventDate`/`EffectiveDate`/`WorkDate` 等）。

**分类**: 框架使用错误，`DateOnly` 被过度推广到不应使用的字段上。修正后编译无报错。

---

### 2.17 resolveTargetName 绕过软删除泄露已删除实体名称

**根因**: `file_service.go` 中 `resolveTargetName()` 使用 `dao.DB.Table("xxx")` 直接查表，GORM 的 `deleted_at IS NULL` 只能作用于 Model 级查询，裸表查询无软删除过滤。已软删除的人员/公司/事件名称仍显示在文件的"关联对象"列表中。

**处理**: 全部 10 处 `Table()` 查询追加 `WHERE deleted_at IS NULL` 条件。

**分类**: 框架使用细节漏洞。与级联软删除配合可彻底消除——级联后 file_relations 本身被软删除，GORM 自动过滤。

### 2.18 考勤模型重构为天维度主表+事件详情子表

**根因**: 原始 `attendance_events` 单表无法表达每日多条事件与日级别 pending/confirmed 状态。

**处理**: 拆分为 `attendance_daily`（天主表）+ `attendance_event_details`（详情）。pending 状态传播到 `attendance_daily_projection`，核算检查投影层。确认操作事务替换 details 并切状态。

**分类**: 架构升级，3 个旧文件替换，15 处查询点同步更新。

---

## 三、架构评估总表

| 架构 | 统一性 | 复用性 | 可维护性 | 评分 |
|------|--------|--------|---------|------|
| L0→L3 投影模型 | ★★★★★ | ★★★★★ | ★★★★★ | 系统基石，所有核算链路依赖 |
| 统一 seq 生成 | ★★★★★ | ★★★★★ | ★★★★★ | 4 表共享同一模式，零冲突 |
| 时段快照算法 | ★★★★★ | ★★★★☆ | ★★★★★ | 3 个投影表复用 |
| 通用组件体系 | ★★★★☆ | ★★★★★ | ★★★★☆ | ProTable 的 SearchField 还可扩展 |
| GORM 审计 hooks | ★★★★★ | ★★★★★ | ★★★★☆ | Callback 模式侵入基础设施但一次注册全局生效 |
| DateOnly 类型 | ★★★★★ | ★★★★★ | ★★★★★ | 全项目统一，零副作用 |
| Sentinel error | ★★★★★ | ★★★★☆ | ★★★★★ | Go 标准实践 |
| 软删除 + 级联 | ★★★★★ | ★★★★★ | ★★★★★ | ID 卡 + seq + 确定性级联 |
| Pending 状态传播 | ★★★★★ | ★★★★★ | ★★★★★ | L0→L1→L2 单向，无跨层查询 |
| 钉钉导入解析 | ★★★★☆ | ★★★☆☆ | ★★★★☆ | 规则完备，按业务维度拆分 pending |

---

## 四、开发统计

| 指标 | 数据 |
|------|------|
| 后端 Go 文件 | ~72 个 |
| 前端 Vue/TS 文件 | ~46 个 |
| API 端点 | 230+ |
| 数据库表 | 36 张 |
| 权限定义 | 44 条（12 模块 × 4 操作） |
| 系统配置项 | 7 条 |
| 通用组件 | 7 个 |
| 单元测试 | 31 个 |
| P0/P1/P2 重构 | 3 轮、60+ 文件修改 |

---

## 五、经验总结

1. **在第一个模块定好模式，后续严格复用**: seq 生成、软删除、分页、投影重建等模式，全项目保持了高度一致性。

2. **类型系统先于业务逻辑**: DateOnly 类型的创建避免了大量日期格式问题，是 ROI 最高的技术决策。

3. **Go error 处理不要走捷径**: 字符串匹配做业务分支是反模式，应使用 sentinel error + errors.Is()。

4. **前端组件接口设计应预设扩展性**: ProTable 的 SearchField 类型系统从初始 4 种逐步扩展到 6 种，若一开始就设计为开放接口会更顺畅。

5. **GORM 的软删除在字段类型上有隐性约定**: `gorm.DeletedAt` 而非 `*time.Time`，这个知识点应在模型创建之初建立规范。

6. **Vue 3 的 setup 阶段初始化时机**: `reactive` 对象应在声明后立即填充默认值，避免模板渲染在 mount 前读取到 undefined。

7. **软删除必须级联**: 不级联软删除会导致(1)查询复杂度失控——每次跨表关联都要检查关联项是否存活；(2)孤儿数据不可清理——CleanOrphanFiles 因 file_relations 未被级联删除而永远无法清理。级联后恢复也级联，是确定性操作。

8. **配置统一路径优于多路查找**: 最初 conf.go 有 3 路查找 + baseDir 状态机（exeDir/cwd/config 子目录）。统一为"配置永远在二进制同级目录"后，代码从 143 行降到 126 行，四个运行场景行为完全一致。

9. **工资核算公式的底层概念必须精确**: 日历天数 vs 计薪天数、加权分母 vs 折算分子、activeDays vs totalDays，概念混淆会导致全月超额发放或离职月补贴消失。最终正确模型：全月 `wXxx/activeDays`，非全月 `wXxx/salaryDays`，代扣 `wXxx/activeDays` 永远不折算。

10. **考勤模型应按业务领域拆分而非技术便利**: 原始单表设计在引入 pending 确认流程后暴露了粒度不匹配的问题。天维度主表+事件详情子表的拆分更贴合实际业务场景，后续扩展（如按天做审批流）也更容易。
